# 상용 출시 잔여 과제 (COMMERCIAL READINESS)

작성 2026-09-01. **이 문서는 자동 개발의 최우선 백로그다.** 위에서부터 소진한다.

## 원칙
- `[ ]` 미완, `[x]` 완료. 완료 시 근거(파일·테스트)를 한 줄로 남긴다.
- **build now, activate on approval**: 코드는 끝까지 만들되 실인증·실결제·실개인정보·실발신 **활성화는 사람 승인**. 스위치는 환경변수로 분리하고 기본 OFF.
- 임의 성과·KPI 수치를 화면·문서에 넣지 않는다. 실측 전에는 기능 서술로 쓴다.
- 모든 변경은 테스트·빌드 검증 통과 후 커밋한다.

## 공통 상용 필수 (전 제품)
- [x] **에러 모니터링** — 전역 에러 캡처 + 알림 훅. DSN은 환경변수, 미설정 시 무해하게 no-op
  - 근거: `src/lib/monitoring.ts`(DSN 없으면 no-op·전송 전 PII 마스킹·예외 흡수), `/api/chat` 오류 보고 + 안전 폴백, 테스트 5건
- [x] **구조화 로깅** — 요청 ID·소요시간·에러코드. PII 미기록
  - 근거: `src/lib/logger.ts`(한 줄 JSON, 필드 화이트리스트 — 대화 본문·연락처·세션ID 원문 기록 불가, 세션은 해시), `/api/chat`·`/api/escalation` 연동 + 응답 `x-request-id`, 런타임 테스트 10건(허용 목록 밖 필드 차단·마스킹·5xx 레벨·중복 기록 방지 포함)
- [x] **/health 확장** — 의존성(DB·외부API) 상태와 버전·커밋 해시 노출(민감정보 제외)
  - 근거: `/api/health`가 flags(kakaoLive·llm·adminAuthRequired·adminTokenConfigured·monitoring)·env·commit 7자리·`dependencies.storage`(드라이버·네임스페이스별 저장 상태·마지막 오류 요약) 노출. 저장 실패 시 `status: 'degraded'`. 외부 DB·외부 API 의존성은 아직 없다 — 도입 시 dependencies에 추가한다
- [x] **표준 에러 응답** 전 API 통일 + 입력검증
  - 근거: `src/lib/http.ts`(code/error/message 3필드·본문 크기 상한·optStr/reqStr 검증). 전 13개 라우트가 `@/lib/http` 사용(카카오 웹훅만 채널 규격상 별도 응답 포맷 유지)
- [x] **rate limit** 공개 API 적용
  - 근거: `src/lib/ratelimit.ts` + 적용 — chat 60/분, escalation 20/분, kakao webhook, admin auth, shared/scenario 30/분. 전역 저장소(Redis) 연동은 [승인 필요]
- [x] **접근·감사 로그** — 관리 기능 접근 이력
  - 근거: `src/lib/audit.ts`(9종 액션·인증 여부만 기록·시크릿 미기록) + `/api/admin/audit` 조회·CSV. 영구 저장은 [승인 필요]
- [x] **백업·복구 절차** RUNBOOK.md 문서화 + 복구 리허설 기록
  - 근거: `RUNBOOK.md`(상태 점검·백업·복구·롤백·리허설 기록) + `tests/runtime.test.mjs` 복구 리허설 3건(정상 복구·손상 스냅샷 6종 거부·무효 항목 스킵)이 매 실행 검증. 운영 인스턴스 실기동 리허설은 [승인 필요]
- [ ] **약관·개인정보 처리방침 확정본 반영** (현재 초안, 문안은 사람이 확정) — **[승인 필요]** 자동 확정 불가
- [x] **테스트** 핵심 로직 커버리지 확보, CI에서 실행
  - 근거: `.github/workflows/ci.yml`(npm ci → typecheck → test → build → route export 규칙 검사), 테스트 94건(정적 계약 53 + 런타임 동작 41). 런타임 테스트는 `tests/_compile.mjs`가 TS를 실제 컴파일해 실행

## 챗봇 전용 (준비도 ~45%)
- [x] **데이터 영속화** — 저장소 어댑터로 KB·룰·티켓·대화로그 연결
  - 근거: `src/lib/storage.ts`(드라이버 memory|file·원자적 쓰기(tmp→rename)·디바운스·flushSaves·실패 시 상태+로그+모니터링 기록, 읽기전용 FS는 오류와 구분), 4개 네임스페이스 연결(`adminStore`·`audit`·`escalation`·`convlog`), `/api/health`의 `dependencies.storage`와 관리 콘솔 "저장소 상태" 패널(빈 상태·승인 대기·읽기전용·오류 화면)에 노출. 테스트 13건(정상 저장·복원, 쓰기 실패, 읽기전용 분류, 손상 파일, PII 게이트, 비활성, flush, 재기동 복원)
  - 개인정보 포함(`tickets`·`convlog`)은 `PERSIST_PII=true` **[승인 필요]** 전까지 디스크에 쓰지 않는다(코드 완성·스위치 잠금). DB 드라이버는 `StorageDriver` 인터페이스만 준비 — 도입은 인프라 확정 후
- [x] **LLM 어댑터 완성** — 프로바이더 인터페이스, 실패 폴백, 토큰 상한 **[CHAT_LLM_LIVE 활성화는 승인 필요]**
  - 근거: `src/lib/llm.ts`(Anthropic·OpenAI 호환 2종 프로바이더 · 게이트 OFF면 네트워크 호출 자체를 안 함 · 전송 전 PII 마스킹 · 입력 문자/출력 토큰/분당 호출 3중 상한 · 429·5xx만 재시도 · 연속 실패 시 서킷 차단 · **throw 없음**), `src/lib/chat.ts`의 `replyToAsync`(근거 자료 없으면 생성 안 함 = 환각 방지, 실패 시 결정적 폴백 복귀 + `llmFailure` 사유 기록, 성공 시 AI 생성 고지 부착), `/api/chat`·카카오 웹훅이 사유를 구조화 로그에 기록. 런타임 테스트 12건(정상·5xx 재시도·4xx 미재시도·타임아웃/네트워크 구분·키 미설정·호출 상한·서킷·입력 절단·폴백 복귀·고지 부착)
- [x] **관리자 인증 실전환 준비** **[ADMIN_AUTH_REQUIRED 전환은 승인 필요]**
  - 근거: 관리 토큰 비교를 상수 시간으로 교체(`src/lib/http.ts` → `safeEqual`, 타이밍으로 값이 새지 않는다), 실패 누적 잠금 `src/lib/adminAuth.ts`(임계 도달 시 잠금 · 잠긴 동안 카운트 미증가 · 성공 시 즉시 해제 · IP별 분리 · 토큰 값 미보관), `/api/admin/auth`가 잠금 상태·남은 시도를 한국어 사유로 반환하고 관리 콘솔 잠금 화면이 그대로 표시. 테스트 2건(런타임 잠금 시나리오 + 계약 검사)
  - 남은 범위: 잠금의 **전역 공유**(현재 인스턴스 단위)와 세션 토큰 회전은 공유 저장소 도입 후 — [승인 필요]
- [x] **카카오 채널 웹훅 서명 검증·재시도 처리** **[실연동은 승인 필요]**
  - 근거: `src/lib/webhookAuth.ts`(HMAC-SHA256 `v1:timestamp:body` 서명 · `timingSafeEqual` 상수 시간 비교 · 허용 시간창 밖 요청 거절 = 리플레이 차단 · TTL 멱등 캐시), `src/lib/kakao.ts`의 `authenticateKakao`(스킬 토큰 + 서명, `KAKAO_SIGNATURE_REQUIRED=true`인데 시크릿이 없으면 **전면 차단**), `src/app/api/kakao/webhook/route.ts`(원문 기준 검증을 위해 `readJsonWithRaw` 사용, 검증 실패는 401이며 사유는 응답에 미노출, 중복 전달은 엔진 재실행 없이 이전 응답 반환 → 티켓·대화로그 중복 방지, 엔진 오류는 모니터링 보고 후 안전 말풍선). 테스트 8건(정상 서명·본문 변조·시크릿 불일치·리플레이·누락 3종·중복 전달·상수 시간)
  - 남은 범위: 중복 판정 캐시가 인스턴스 단위 — 전역 멱등은 공유 저장소 도입 후 [승인 필요]
- [x] **대화 품질** — 멀티턴 슬롯 수집, 오타·동의어 보정, 근거 인용 표시
  - 근거(슬롯 수집): `src/lib/slots.ts`(폼 정의·항목별 검증·"취소/이전/건너뛰기" 제어어·같은 항목 3회 실패 시 `max_retry` 상담원 이관), `src/lib/chat.ts` 통합(룰 인텐트 → 접수 폼 자동 시작, 완료 시 티켓 생성 + 이관 요약에 수집 항목 첨부, 수집 중 "상담원" 요청은 즉시 이관). 게이트 `CHAT_SLOT_FORMS=false`로 운영 중 즉시 회피 가능(기본 ON). 현재 폼: 예약 접수(성함·희망 일시·연락처), 장애 신고(증상·발생 채널·연락처 선택)
  - 근거(오타·동의어): `src/lib/normalize.ts` 자모 편집거리 + 동의어 그룹 17종, 매칭 종류별 가중치(정확>동의어>오타)
  - 근거(근거 인용): `knowledge.buildCitation`이 KB 원문 문장을 그대로 인용(생성 요약 아님), 위젯 "근거" 박스에 출처·문장 노출
  - 화면: 위젯 진행 표시(단계 바 + `role="status"`·`aria-live` + 총 단계·현재 항목을 읽는 aria-label, 이전/건너뛰기/취소 버튼은 마지막 턴에만 노출), 잘못된 입력은 "무엇이 왜 틀렸는지 + 입력 예시"로 되묻는다
  - 개인정보: 연락처 슬롯은 확인 문구·이관 요약에서 마스킹(`handoff.maskPii`). 수집값 영구 저장은 기존 `PERSIST_PII` **[승인 필요]** 게이트 적용
  - 회귀 수정: 날짜(`2026-09-04`)가 계좌번호 패턴으로 오인돼 예약 일시가 `***-****-****`로 뭉개지던 마스킹 버그 수정(`handoff.ts` 보호 규칙 + 회귀 테스트)
  - 테스트 14건 추가(정상 3단계 수집 완료·재시도 한도 이관·취소 후 복귀·상담원 요청 이관·증상 오탐 방지·게이트 OFF·날짜 파싱·선택지 번호/동의어·검증 실패 안내·필수 건너뛰기 거부·이전 되돌리기·마스킹 회귀)
- [ ] AICC-Core 연계 — Flow 노드·채널 렌더러 스키마 정합
- [x] 테스트 확대 — **정적 계약 58건 + 런타임 동작 62건 = 120건**(TS를 실제 컴파일해 실행: 로깅·복구 리허설·LLM 어댑터·대화 엔진 LLM 폴백·웹훅 서명·관리자 잠금)

## 파트너 채널 (제이투모로우원 — 운영 대행 + 수익 배분)

계약·서비스 주체는 고원, 파트너는 영업·운영을 담당하고 수익을 배분한다.
**향후 리셀러(파트너 명의 계약)로 전환될 수 있으므로, 지금은 2계층으로 확장 가능한 형태로만 열어둔다.**

- [x] **파트너(채널) 개념 도입** — 조직/계약에 `partner_id`(nullable) 추가. 없으면 직접 계약. 스키마만 준비하고 화면 노출은 최소
  - 근거: `src/lib/partners.ts`(`Partner`·`Account.partnerId: string | null` — null이면 직접 계약이라 파트너를 몰라도 기존 동작 그대로), `partners` 저장 네임스페이스(개인정보 없음 → 승인 없이 저장), `/api/admin/partners`(GET·POST·DELETE 모두 `requireAdmin`), 관리 콘솔 "파트너·귀속" 탭(빈 상태·오류·불러오는 중 화면, 라벨 연결된 폼, 표 `<th scope>`)
  - 개인정보 최소화: 담당자는 **이름만** 저장하고 전화·이메일 필드를 두지 않는다(테스트로 고정). 파트너 삭제는 연결 고객사가 있으면 거절하고, 삭제 전 확인 창을 거친다
- [x] **매출 귀속 근거** — 어떤 고객사가 어느 파트너를 통해 유입됐는지 기록(유입 경로·계약일·담당자)
  - 근거: `Account`에 `source`(직접 영업·파트너 유치·고객 소개·인바운드·미확인)·`contractedAt`·`ownerName`, 귀속 변경은 **append-only 이력**(`attribution`: 이전/이후 파트너·경로·사유·인증 여부·시각)으로 남는다. 값만 덮어쓰면 근거가 사라지므로 변경 시 이력 추가를 강제
  - 검증 규칙: 계약 상태는 계약일 필수, 계약일은 실재하는 날짜만(2026-02-31 거절), 경로가 "파트너 유치"면 파트너 지정 필수, 없는 파트너 지정 거절 — 실패는 이유 문구로 반환
  - 고객사 **삭제 API는 만들지 않았다**(해지는 `status='churned'`) — 정산 근거를 지울 수 있는 경로를 열지 않는다. 백업(`/api/admin/backup`)에 `partners` 키로 포함되며 예전 백업과 하위 호환
  - 수수료율은 설정값(`partner.feeRateBp` → `PARTNER_DEFAULT_FEE_RATE_BP`), 미설정이면 `null`·화면 "미설정". 집계는 **건수만** 센다(금액·성과 수치 없음)
- [ ] **파트너 역할 권한** — 파트너 담당자는 자기가 유치한 고객사만 조회. 기존 RBAC에 `partner_admin` 역할 추가(활성화는 승인)
- [ ] **정산 리포트** — 파트너별 계약·이용 실적·수수료 산출 근거를 조회·내보내기. 수수료율은 설정값으로 분리(하드코딩 금지)
- [x] **2계층 확장 여지 확보** — 조회 경로에 파트너 필터가 나중에 끼어들 수 있도록 쿼리 계층 정리. 화이트라벨은 구현하지 않음
  - 근거: 고객사 조회는 전부 `queryAccounts(filter)` 한 함수를 지난다(파트너·상태·경로·이름 필터). 리셀러 전환 시 "파트너 담당자는 자기 고객사만" 권한 필터를 이 함수 한 곳에만 끼우면 된다. 라우트·화면은 이 함수 결과만 사용
  - 테스트 12건(직접 계약 기본값·귀속 이력 누적·불변 수정은 이력 미증가·검증 실패 6종·삭제 보호·필터·수수료 설정값·집계 건수·스냅샷 복원(손상 건너뛰기·고아 귀속 복구·id 충돌 방지))

> 원칙: 파트너 관련 기능도 **코드는 만들되 활성화는 승인**. 실제 정산·청구는 계약서 확정 후.

