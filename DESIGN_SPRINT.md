# 챗봇 디자인 스프린트 — AICC Portal 수준으로 (최우선 백로그, 9/9~9/16)

> 진단(9/9): 기능·테스트는 충분(156건)하나 화면이 개발자 초안 수준. 콘솔에 내부 문구 노출, 대시보드는 텍스트만, 랜딩은 히어로 하나.
> 목표: **AICC Portal(callbot-portal)과 같은 라인의 제품처럼 보일 것.** 디자인 시스템을 이식한다 — 새로 만들지 않는다.

> 진행(9/10): 디자인 토큰을 `src/app/globals.css` 로 이식 완료 — 화면은 모두 var() 참조라 랜딩·콘솔·위젯에 동시에 반영된다.
> 진행(9/10, 2회차): 1순위 완료(상담원 전환 카드·임베드 깜빡임). 2순위는 셸·대시보드까지 완료.
> 진행(9/10, 3회차): 최근 대화 상세 서랍·지식베이스 표/편집 폼·응답 테스트 분할 완료. 남은 것은 룰 카드 빌더·로그인 화면.
> 진행(9/14): 룰 카드 빌더·로그인 화면 완료 — **2순위 전부 완료.** 스프린트 문서 소진, 다음은 `EUM_INTEGRATION.md`.
> 진행(9/14, 2회차): 백로그 문서 3종이 모두 소진돼 **디자인 스프린트 이전에 만들어진 콘솔 탭 3개**(상담원 요청·감사 로그/저장소·테넌트 지식)를 같은 규격으로 끌어올렸다 — 아래 「2순위 후속」.
> 진행(9/14, 3회차): 「사업」 그룹 2개 탭(파트너·귀속, 정산 리포트)까지 같은 규격으로 — **콘솔 10개 탭 전부 스프린트 규격.** 남은 것은 [승인 필요] 항목뿐(약관 확정본).
> 진행(9/16): 보류했던 2건을 닫았다 — 평균 응답 시간 실측(DS 2-14)·헤더 전역 검색(DS 2-15). 「2순위 후속」에 기록.
> 진행(9/16, 2회차): 완료 기준으로 다시 훑어 남은 "개발자 초안" 흔적 3곳을 고쳤다 — 기본 Next 404/오류 화면, 불러오는 동안 먼저 뜨던 「자료가 없습니다」 빈 상태, 오프라인 무반응. 아래 「4순위 — 마감 점검」.
> 진행(9/16, 3회차): 랜딩·콘솔 밖에 남아 있던 두 곳 — 약관·방침 페이지(상단바 없이 본문만, 이모지 경고)와 링크 미리보기(OG 이미지·테마색 없음, 콘솔이 색인 가능) — 를 같은 규격으로. 아래 DS 4-4·4-5.

> 진행(9/17, 3회차): 콘솔의 **쓰기·내려받기 경로**를 6조건으로 훑어 3건 — 내려받기 4곳이 관리 토큰을 주소에 싣던 문제(DS 5-8), 백업 복원에 확인 절차가 없던 문제(DS 5-9), DS 5-4 가 놓친 쓰기 3곳의 실패 삼킴·세션 만료(DS 5-10).

> 진행(9/17, 2회차): 백로그가 모두 닫힌 뒤 6조건으로 다시 훑어 3건 — 위젯 전송 실패 복구(오류가 답변처럼 보이던 문제·「다시 보내기」·오프라인·글자 수), 건너뛰기 링크와 랜드마크 구조, 공개 약관/방침의 내부 표기. 아래 DS 5-5·5-6·5-7.

> 진행(9/18): 백로그 3종이 모두 닫힌 뒤(EUM_INTEGRATION 완료, COMMERCIAL_READINESS 는 [승인 필요] 1건만 남음) 6조건으로 다시 훑어 3건 — 문서 업로드 폼이 콘솔에 하나 남은 개발자 초안(DS 6-1), DS 5-4 가 세운 색 토큰을 우회하던 하드코딩 28곳(DS 6-2), 탭이 주소에 남지 않아 새로고침·뒤로가기에서 자리를 잃던 문제(DS 6-3). 아래 「6순위」.

## 디자인 시스템 (AICC Portal 에서 이식)
- 색: primary #2563EB, ink #0F172A, sub #475569, line #E2E8F0, bg #F8FAFC, success #16A34A, warn #D97706, danger #DC2626
- 폰트 Pretendard, 라운드 14~18px 카드, 그림자 `0 1px 2px rgba(15,23,42,.04), 0 12px 30px -18px rgba(15,23,42,.18)`
- KPI 카드: 라벨 12.5px/값 30px 800/변화량 pill. 표: 헤더 11.5px 대문자 느낌, 행 hover
- 참고 파일: `Dev/2. Callbot/v2/callbot-portal/public/index.html`(랜딩·KPI·차트), `public/admin.html`(콘솔 셸·사이드바·표)

## 1순위 — 채팅 위젯 (이음 임베드 → 2R 평가자가 봄)
- [x] 헤더: 아바타(원형·브랜드색)·이름·「AI가 응대합니다」 배지·최소화/닫기. 테넌트 색상(`eum`=#BE5535) 적용
  - 근거(9/10): 36px 원형 아바타+테넌트 badge, AI 배지(점+라벨), 최소화(대화 유지)/닫기(초기화) 분리. `brandVars()`가 --brand 계열만 덮어써 렌더 HTML에 `--brand:#BE5535` 확인(tests/widget.test.mjs)
- [x] 말풍선: 봇/사용자 구분 색·꼬리·시각, **타이핑 인디케이터(점 3개)**, 도착 애니메이션
  - 근거(9/10): 봇=흰 카드+좌하 꼬리(radius 4)·사용자=브랜드+우하 꼬리, 말풍선 하단 시각(마운트 후 렌더 — hydration 불일치 방지), `.gw-dot` 점 3개(aria-label "답변을 작성하고 있습니다"), `.gw-rise` 도착 애니메이션 + prefers-reduced-motion 존중
- [x] 빠른 답장 칩(FAQ 상위 4개), 답변 하단 **근거 표시**(FAQ 번호·출처)와 「도움이 됐나요 👍👎」
  - 근거(9/10): `tenantConfig()`가 실제 적재 KB에서 상위 4건 질문을 그대로 내려보냄(문구 하드코딩 금지 테스트 동반). 근거 카드는 브랜드 좌측선+FAQ 라벨. 평가는 `/api/feedback`에 기록(근거 라벨·세션 해시만 저장, 대화 본문 미전송), 실패 시 재시도 안내
- [x] 상담원 전환 UI: 버튼 → 연락처 입력 카드 → 접수 완료 상태. 오류 시 재시도
  - 근거(9/10): 「상담원 연결하기」가 곧바로 접수하지 않고 카드를 연다 — 연락처 입력(전화/이메일 형식 검사)·이용 목적/파기 안내(`aria-describedby`)·「연락처 없이 접수」·취소. 접수 중에는 버튼 잠금+`aria-busy`(중복 접수 차단), 실패는 `role="alert"`+「다시 시도」, 완료 카드에 접수번호·상태·접수 순번. `/api/escalation` 의 `contact` 계약 그대로 사용(서버 무변경)
- [x] 모바일 375px 전체화면 모드, 키보드 포커스 트랩·ESC 닫기·aria-live
  - 근거(9/10): 480px 이하 전체화면 시트(임베드는 embed.js가 호스트 폭을 알려줌 — iframe 폭은 뷰포트가 아니므로). role=dialog + Tab 순환 + ESC 최소화 + 대화 목록 role=log/aria-live=polite, 닫은 뒤 초점은 런처로 복귀
- [x] `embed.js`: 로드 시 깜빡임 없음, `data-tenant`·`data-position` 옵션 유지
  - 근거(9/10): v0.5 — iframe을 `opacity:0` 으로 붙였다가 위젯이 보내는 `ready` 신호에 맞춰 페이드인. 신호가 막혀도 load+400ms·하드 2500ms 폴백으로 반드시 보인다(위젯이 사라지는 실패 모드 없음). `prefers-reduced-motion` 이면 전환 없이 즉시 표시. `data-tenant`·`data-position`·`data-offset`·`data-z` 계약 유지

## 2순위 — 관리 콘솔 (/admin)
- [x] 콘솔 셸: 좌측 사이드바(아이콘+라벨, 현재 탭 강조)·상단 헤더(테넌트·검색·관리자)·본문 카드 그리드
  - 근거(9/10): 가로로 늘어선 버튼 10개 → 236px 사이드바(로고·4개 그룹「운영/콘텐츠/사업/설정」·선 아이콘 10종·`aria-current="page"` 강조). 상단 스티키 헤더에 현재 화면 제목+한 줄 설명(`TAB_DESC`)·인증 배지·관리 토큰(보이지 않는 라벨 연결). 저장 결과는 인라인 문구 대신 토스트(`role="status"`)
  - 375px: 사이드바가 상단 가로 스크롤 메뉴로 접힌다(`.ac-navgroup{display:contents}`·그룹 제목 숨김·버튼 min-height 38px). 렌더 테스트 `tests/console.test.mjs`
  - 검색은 별도 항목으로 분리 → DS 2-15 에서 완료(헤더 전역 검색)
- [x] 대시보드: KPI 4(오늘 대화·자동완결률·상담원 전환·평균 응답) + **스파크라인/막대 차트**(Chart.js CDN 허용). 값 없으면 「측정 중」— 가짜 수치 금지
  - 근거(9/10): `convStats()` 에 실제 로그 기반 `daily`(최근 7일, 한국 시간 기준)·`today` 집계를 추가하고 KPI 4장(라벨 12.5/값 30px 800)을 그렸다. 평균 응답 시간은 수집 경로가 없어 「측정 중」, 자동완결률도 대화 0건이면 「측정 중」(0% 로 단정하지 않음)
  - 차트는 CDN 없이 인라인 SVG 막대(대화/상담원 제안 2계열, `role="img"` + 전체 수치를 담은 aria-label). 7일 내 기록이 없으면 차트 대신 「측정 중」 안내
  - 응답 근거·채널·인텐트를 내부 코드(`kb`/`web`) 대신 사람 말(「등록 자료」·「홈페이지」)로 표시 — 렌더 테스트에서 `intent `·`source ` 노출 0건 고정
- [x] 최근 대화: 목록 → 클릭 시 우측 **상세 서랍**(전체 대화·의도·출처·전환 여부)
  - 근거(9/10): 대시보드 최근 대화가 한 줄 요약 행(고객 말·챗봇 답·시각·주제·근거·상담원 제안)으로 바뀌고, 행을 누르면 우측 520px 서랍이 같은 대화(세션)의 흐름 전체를 말풍선으로 보여준다. 상단 요약 pill(메시지 수·자동 완료/상담원 제안·접수 상태), 하단에 「상담원 요청 보기」(해당 접수가 있을 때만 → 상담원 요청 탭). 대화 식별자는 앞 6자만 노출. 빈 상태는 일러스트+「응답 테스트 열기」
  - 접근성: `role=dialog`+`aria-modal`, 열리면 닫기 버튼으로 초점 이동, Tab 순환, ESC/배경 클릭으로 닫힘, 닫으면 연 행으로 초점 복귀. 375px 는 전체폭 시트·행은 세로 배치. `prefers-reduced-motion` 에서 슬라이드 제거. 서버 무변경(기존 `recentTurns` 30건 안에서 세션별로 묶음)
- [x] 지식베이스: 표(검색·태그 필터) + 우측 편집 폼, 저장 토스트, 빈 상태 일러스트
  - 근거(9/10): 카드 나열 → 좌 표(카테고리 pill·질문/답변 2줄 요약·출처·키워드·수정/삭제) + 우 스티키 편집 폼. 툴바에 검색(질문·답변·키워드·출처)과 카테고리 셀렉트(실제 등록값에서 생성), 건수 「n/전체」. 편집 중인 행 강조. 빈 상태 2종(자료 0건 → 일러스트+「기본 자료 불러오기」, 검색 0건 → 「필터 지우기」)
  - 폼: 모든 입력에 `<label>`, 필수(질문·답변) 인라인 오류+`aria-invalid`/`aria-describedby`, 저장 중 버튼 잠금(`aria-busy`), 네트워크 실패 안내, 식별자는 「고급」 접힘. 삭제·초기화는 `confirm` 을 거친다(QUALITY_BAR §3). 문서 붙여넣기 섹션은 표 아래로 이동(문구 사용자 언어로). 375px: 한 단으로 접히고 키워드 열 숨김, 수정 시 폼으로 스크롤
- [x] 시나리오 룰: 조건→응답 카드 빌더, 미리보기
  - 근거(9/14): 카드 나열 → 좌 「내가 만든 규칙」·「기본 규칙」 카드 목록(검색 툴바·건수). 카드마다 「고객이 이렇게 말하면」 표현 칩 → 화살표 → 「이렇게 답합니다」 흐름, 켜기/끄기는 `role="switch"`+`aria-checked`+이름 있는 스위치, 상담원 연결·답변 수정됨 pill. 기본 규칙은 정규식 대신 사람이 읽을 예시 표현 칩으로 풀고(`patternExamples`) 답변만 인라인 편집(벗어나면 저장 안내)
  - 우 스티키 빌더: 규칙 이름 → ① 표현(쉼표 구분, 입력 즉시 칩 미리보기) → ② 답변·상담원 접수 체크. 필수 3항목 인라인 오류(`aria-invalid`/`aria-describedby`), 저장 중 `aria-busy` 잠금, 네트워크 실패 안내, 삭제는 `confirm`. **미리보기**: 시험 문장을 적으면 어떤 표현으로 적용되는지(적중 칩 강조) + 고객에게 보일 말풍선(`role=log aria-live`)을 그리고, 근사치임을 밝히며 최종 판정은 「응답 테스트」로 안내. 서버 무변경
  - 내부 용어 제거: 「커스텀 룰」「정규식」「패턴: /…/」「(intent)」 화면 문자열 0건(감사 로그 라벨·복원 토스트 포함) — 테스트 고정. 375px: 조건/응답 세로 쌓임·화살표 회전, reduced-motion 시 스위치 전환 제거
- [x] 응답 테스트: 좌 입력/우 위젯 미리보기 분할
  - 근거(9/10): 좌: 「고객이 보낼 말」 textarea(Enter 전송·Shift+Enter 줄바꿈·IME 조합 중 전송 방지) + 「왜 이렇게 답했나」(주제·근거·인용·확신도 — `intent:`/`source:` 내부 코드 표기 제거, 라벨 사전 재사용). 우: 위젯과 같은 규격의 상담창 미리보기(브랜드 헤더·아바타·AI 배지·말풍선·근거 인용·`gw-dot` 타이핑 인디케이터, `role=log aria-live`). 대기 중 버튼 잠금, 네트워크 실패 시 말풍선으로 안내. 개인정보 주의 문구. 375px 한 단·미리보기 높이 300px
  - 실제 iframe 대신 같은 `/api/chat` 응답을 위젯 규격으로 그린다 — 테스트 세션이 위젯 세션과 섞이지 않고, 좌측 입력이 우측 화면을 직접 구동한다
- [x] **내부 문구 전부 제거**("data/admin-store.json", "401이면", "서버 메모리 기준"). 필요한 안내는 사용자 언어로 툴팁/설정 페이지에
  - 근거(9/10): 콘솔 부제·통계 로딩·통계 각주 3곳을 사용자 언어로 교체. 주석은 허용하되 **화면 문자열**에 내부 문구가 없음을 테스트로 고정
  - 덤: 「설치」 탭 신설 — 랜딩에 있던 임베드 스니펫을 옮기고 배포 주소는 `window.location.origin` 으로 생성(하드코딩 금지)·복사 버튼·옵션 표
- [x] 관리 토큰 입력은 「로그인」 화면으로 (ADMIN_AUTH_REQUIRED 는 여전히 [승인 필요])
  - 근거(9/14): 헤더의 토큰 입력칸 제거 → 상태 배지(로그인됨/로그인 전/인증 미설정) + 「로그인」/「로그아웃」(토큰 삭제 후 로그인 화면). 잠금 화면(🔒 텍스트)을 브랜드 마크·제목·라벨 있는 토큰 입력(보기/숨기기 `aria-pressed`)·`<form onSubmit>`(Enter)·오류 `role=alert`+`aria-invalid`·확인 중 `aria-busy` 인 로그인 카드로 교체. 인증이 필수가 아닌 환경에서는 「로그인하지 않고 돌아가기」. 내부 문구(localStorage·401) 0건. 기존 `/api/admin/auth` 계약·잠금(lockout) 그대로

## 2순위 후속 — 스프린트 이전에 만든 탭 (9/14 진단: 카드 나열·내부 문구·세션 원문 노출)
- [x] 상담원 요청: 요약 KPI 4 → 상태 필터 칩 + 검색 → 표 → 접수번호 클릭 시 상세 서랍
  - 근거(9/14): 티켓마다 카드 → 요약(대기/상담 중/완료/자동 응대 완료율 — 대화 0건이면 「측정 중」) + 표(접수 앞 8자·상태 pill·고객 마지막 말 2줄·사유·시각·다음 처리 버튼 1개). 상태 필터는 `aria-pressed` 칩, 검색은 라벨 있는 `type=search`, 건수 `aria-live`. 서랍(`role=dialog`·ESC·Tab 순환·초점 복귀)에 고객 말풍선·사유·이관 요약·**연락처 마스킹(「보기」를 눌러야 원문)**·상태 변경(`aria-busy`)·「대화 전체 보기」(대화 서랍으로 연결). 내부 문구 「서버 메모리에만 저장됩니다」 제거 → 저장 미승인일 때만 사용자 언어 안내. 세션 원문·티켓 전체 id 노출 0건. 375px: 사유·시각 열 접힘·서랍 전체폭
- [x] 감사 로그·저장소 상태: 표(시각·작업 pill·대상/내용·인증 pill) + 작업 종류 필터 + 저장소 네임스페이스 카드 그리드(상태별 색)
  - 근거(9/14): 이벤트마다 카드 → 표. 빈 상태 일러스트+「지식베이스 열기」, 필터 0건 「필터 지우기」. 저장소 힌트의 환경변수명(`ADMIN_PERSIST`·`PERSIST_PII`) 제거 → 사람 말 + [승인 필요] 표기. 테스트가 화면 문자열의 환경변수명·내부 경로 0건 고정
- [x] 테넌트 지식: 요약 카드 4(상담창 이름+브랜드 색 견본·적재 FAQ·신청 버튼 주소와 출처 pill·AI 고지) + FAQ 표(근거 라벨·질문/카테고리·답변 2줄·표현 수)
  - 근거(9/14): `data/<테넌트>-faq.json` 경로 문구 제거. 0건은 경고 빈 상태. 편집 입력 없음(읽기 전용 유지 — 테스트 고정). 「코드 기본값 — 배포 환경변수 미설정」 → 「기본값 — 배포 설정 미등록」
- [x] 파트너·귀속: 요약 KPI 4 → 좌 고객사 표(검색·귀속 필터)·파트너 표 / 우 스티키 폼(고객사↔파트너 전환) → 고객사명 클릭 시 상세 서랍(계약 정보·귀속 이력 타임라인)
  - 근거(9/14): 세로로 쌓인 카드 6장(집계·폼 2개·목록 2개) → KPI(운영 중 파트너·고객사·계약 중·검토 중, 불러오기 전 「측정 중」) + `ac-split`. 고객사 표는 상태 pill·귀속·유입 경로·계약일·월 이용료(미입력은 흐리게), 파트너 표는 상태 pill·수수료율·담당·귀속 고객사 수(집계 표를 파트너 표에 합침, 직접 계약은 마지막 행). 식별자 원문(`p.id`·`a.id`·이력의 partnerId) 화면 노출 0건 — 전부 이름으로. 「bp」 단위 제거 → 수수료율은 %로 입력하고 저장 시 bp 변환(API 계약 무변경)
  - 폼: 고객사/파트너 세그먼트(`aria-pressed`), 모든 입력에 `<label>`, 인라인 오류 6종(`aria-invalid`/`aria-describedby` — 고객사명·파트너 유치면 파트너 필수·계약일 형식·계약 상태면 계약일 필수·월 이용료 숫자·수수료율 0~100), 계약일은 `type=date`, `<form onSubmit>`(Enter), 저장 중 `aria-busy` 잠금, 네트워크 실패 안내. 수정 시 해당 행 강조(`data-editing`)+폼으로 스크롤. 서랍은 `role=dialog`·ESC·Tab 순환·초점 복귀·「수정」으로 폼 연결. 빈 상태 3종(고객사 0·파트너 0 → 「첫 … 등록」, 필터 0건 → 「필터 지우기」). 조회 전용 계정은 폼·수정 버튼 숨김. 375px: 유입 경로·계약일·월 이용료·담당 열 접힘, 폼은 표 아래 한 단. 테스트 `tests/console.test.mjs` DS 2-12
- [x] 정산 리포트: 조건 툴바(기준월·파트너·다시 계산·CSV) → 요약 KPI 4 → 파트너별 합계 표 → 고객사별 산출 근거 표(미산출 사유 pill) → 빈 상태
  - 근거(9/14): KPI(대상 고객사·산출 완료·미산출·수수료 합계) — 리포트 전 「측정 중」, 산출 건이 없으면 합계를 0원으로 보이지 않고 「대상 없음/산출 불가」, 부분 합계는 카드 각주와 경고 카드로 「확정 금액 아님」 명시. 금액은 tabular-nums·우측 정렬, 미산출 건은 경고 pill. 빈 상태는 「파트너·귀속 열기」로 연결, 오류는 `role=alert`+「다시 시도」, 계산 중 `role=status`+버튼 `aria-busy`, 내려받을 것이 없으면 CSV 잠금. 하드코딩 색(#c0392b·#b26a00) 제거. 375px: 계약일·월 이용료·수수료율·기준금액 열 접힘. 테스트 DS 2-13

- [x] 평균 응답 시간 KPI 실측 연결 (DS 2-14)
  - 근거(9/16): `/api/chat`·카카오 웹훅이 서버 처리 시간(`latencyMs`, 네트워크 왕복 제외)을 대화 로그에 기록하고 `convStats()`가 `avgLatencyMs`(표본 0이면 null — 0ms 로 단정하지 않음)·`latencySamples`를 집계. 대시보드 KPI 는 값이 있을 때만 `latencyLabel` 로 표시하고 각주에 「서버가 답을 만드는 데 걸린 시간 · 최근 n건 평균」을 밝힌다. 음수·NaN 지연은 기록 단계에서 버린다. 구버전 로그(지연 없음)는 평균에서 제외. 테스트 `tests/console.test.mjs` DS 2-14 + 런타임 집계 테스트
- [x] 헤더 전역 검색 — 대화·안내 자료·규칙·상담원 요청·고객사·파트너 (DS 2-15)
  - 근거(9/16): `GlobalSearch` — WAI-ARIA combobox(`role=combobox`·`aria-expanded`·`aria-controls`·`aria-activedescendant`) + `role=listbox/option` 결과, 출처별 그룹, ↑↓/Enter/ESC, Ctrl+K 단축키(장식 힌트는 `aria-hidden`, 스크린리더에는 사용법 문장), 건수 `role=status`. 결과를 고르면 해당 탭으로 이동해 서랍/편집 폼을 연다. 고객사·파트너는 검색을 처음 열 때 한 번 불러온다
  - 개인정보: 연락처·세션 원문·접수번호 전체는 색인하지 않는다(요청 서랍에서 「보기」를 눌러야 원문) — 테스트가 색인 함수 본문을 검사해 고정. 375px: 검색칸이 헤더 아래 한 줄 전체폭, 키보드 힌트 숨김

## 3순위 — 랜딩 (/)
- [x] AICC Portal 랜딩 구조 이식: 히어로 → 동작 방식(신뢰 3원칙) → 기능 6 → 채널 3종 → 도입 3단계 → 운영 콘솔 미리보기 → FAQ → CTA
  - 근거(9/10): 상단바(스티키·앵커·CTA) 신설, 히어로에 대화 예시 카드(「화면 예시입니다」 명시) + 실제 위젯은 페이지에 상시 떠 있음. 콘솔 미리보기 KPI 4는 전부 「측정 중」
  - 벤치마킹: 채널톡 알프(자료 등록→근거 기반 응답·멀티채널·즉시 체험), Intercom Fin(답변 근거 인용·전환/해결 지표 중심 설득)
  - **운영자용 정보는 랜딩에서 제거**(사용자 요청 9/10): 임베드 스니펫·`/admin` 링크 → 관리 콘솔 「설치」 탭. 테스트로 재유입 차단
- [x] 브랜드: 제품명을 **GOWON Chat**(영문)으로 통일 + 말풍선 마크 신규 제작. 「AICC 제품군」 배지는 제거
  - 근거(9/10, 사용자 결정): 화면·타이틀·위젯 헤더·약관/방침·임베드 주석까지 「고원 챗봇」 표기 0건. 법인명 「주식회사 고원(GOWON)」은 약관·방침에 그대로 둔다
  - 아이콘: 말풍선 몸통 + 이니셜 G 를 `src/app/icon.svg`(파비콘)로 만들고, 랜딩 상단바·콘솔 사이드바가 **같은 경로 데이터**를 쓰도록 테스트로 고정(브랜드가 화면마다 달라지지 않게). 위젯 기본 아바타는 `G`(테넌트는 자기 배지 유지)
  - 이모지 아이콘(📎🤐🙋📚…) 전량 제거 → 콘솔 메뉴와 같은 굵기(1.4)의 선 아이콘 세트로 교체. 문구도 기계적인 표현을 걷어냈다(「상담 운영에서 실제로 쓰는 것만」·「오픈까지 세 단계」·「도입 전에 많이 받는 질문」 등)
- [x] 가짜 수치·타사 로고 금지(§13). 성능 수치는 기능 서술로
  - 근거(9/10): 「효과는 얼마나 되나요?」 FAQ에서 실측 전 수치 제시를 거절하고 파일럿 집계로 안내. 성과 수치 패턴·화면 내 타사명은 테스트로 금지

## 4순위 — 마감 점검 (9/16: 완료 기준 「같은 회사 제품으로 보이는가」로 다시 훑음)
- [x] 404·오류 화면 브랜드화 — 기본 Next 화면(흰 바탕 영문) 제거 (DS 4-1)
  - 근거(9/16): `src/components/SystemPage.tsx`(카드 규격·브랜드 마크·`h1`+`aria-labelledby`·버튼 42px·줄바꿈) 를 `app/not-found.tsx`(404, 「홈으로」)·`app/error.tsx`(「다시 시도」=reset·「홈으로」)·`app/global-error.tsx`(루트 레이아웃 실패 시 — globals.css 가 없어 토큰 인라인·`lang=ko`) 가 공유. 마크는 `src/components/BrandMark.tsx` 로 파비콘과 같은 경로 데이터(브랜드 테스트에 포함)
  - 오류 내용(메시지·스택·경로)은 화면에 싣지 않고 참조 번호(`digest`)만 — 테스트가 `error.message`/`error.stack` 사용 0건 고정. 404 는 운영자 콘솔 경로를 안내하지 않는다(랜딩과 같은 원칙). 브라우저 오류 보고 훅 연결은 모니터링 DSN 승인 뒤 — [승인 필요]
- [x] 로딩 스켈레톤 — 불러오는 동안 「자료가 없습니다」 빈 상태를 먼저 보이지 않는다 (DS 4-2)
  - 근거(9/16): 데이터 4종(안내 자료·규칙·상담원 요청/현황·변경 이력)에 `phase: loading|done|error` 를 두고, 빈 상태는 `done` 일 때만 그린다. 그 전엔 `SkeletonRows`(막대는 `aria-hidden`, 문장 하나만 `role=status aria-live=polite aria-busy`), 대시보드는 KPI 값 자리·7일 차트 막대까지 스켈레톤. 「측정 중」은 데이터가 *없을 때* 로 의미를 좁혔다(불러오는 중 ≠ 측정 중)
  - 실패 경로: 로더 4개에 try/catch 를 붙여 네트워크 예외가 조용히 사라지지 않게 — `phase=error` 면 `role=alert` + 「다시 시도」. 헤더 토큰 입력이 사라진 뒤에도 남아 있던 「위쪽 관리 토큰을 확인해 주세요」 안내 제거. 파트너·테넌트 탭의 텍스트 로딩도 스켈레톤으로. `prefers-reduced-motion` 에서 반짝임 제거, 375px 차트 스켈레톤 높이 축소. 테스트 DS 4-2(첫 렌더에 빈 상태 0건·상태 기록·게이트 4곳)
- [x] 오프라인 안내 배너 (DS 4-3)
  - 근거(9/16): `online/offline` 이벤트로 헤더 아래 경고 톤 배너(`role=status aria-live=assertive`) — 「인터넷 연결이 끊겼습니다. 화면은 볼 수 있지만 저장·새로고침은 연결이 돌아온 뒤에 됩니다」. 복구되면 사라진다. 서버 렌더에서는 그리지 않는다(연결 상태 미확인). 테스트 DS 4-3
- [x] 약관·개인정보처리방침 페이지 셸 통일 (DS 4-4)
  - 근거(9/16): `LegalLayout` 이 「← 홈」 링크 하나짜리 본문에서 랜딩과 같은 상단바(브랜드 마크 공용 컴포넌트·「홈으로」)·카드(눈썹 라벨·제목·시행일/운영주체/초안 pill)·푸터(약관↔방침 교차 링크)로. 본문 `<h2>` 를 자동으로 색인해 좌측 스티키 **목차**(`nav aria-label="목차"`, `scroll-margin-top`)를 만든다 — 페이지 파일은 제목만 쓰면 된다. ⚠️ 이모지 → 선 아이콘, 하드코딩 색(#9a3412·#ffedd5…) → 토큰(`--warn`) — 테스트가 TSX 의 hex·이모지 0건 고정. 문안(초안·[미확정] 표기)은 그대로 — 확정본 반영은 여전히 [승인 필요]
  - 375px: 목차가 카드 위 가로 스크롤 칩(최소 높이 34px)으로, 카드 여백 18px. 운영자 화면(/admin) 링크 없음
- [x] 링크 미리보기·색인 메타데이터 (DS 4-5)
  - 근거(9/16): 루트 레이아웃에 `metadataBase`(`NEXT_PUBLIC_SITE_URL`, 미설정 시 배포 주소)·Open Graph(`ko_KR`·siteName)·트위터 카드·`viewport.themeColor`(=`--brand`). 미리보기 이미지는 `app/opengraph-image.png`·`twitter-image.png`(1200×630, 파일 규약·대체 텍스트 동반) — `scripts/og-image.py` 가 브랜드 마크 도형·랜딩 히어로 문장·신뢰 3원칙으로 그린다(수치·타사명 없음, 테스트 고정). 관리 콘솔(`admin/layout.tsx`)·임베드 프레임(`/widget`)은 `robots noindex`, 랜딩·약관·방침은 색인 허용

## 5순위 — 백로그 소진 후 재감사 (9/17: 1~4순위가 모두 닫힌 뒤 실제 화면을 6조건으로 다시 훑음)
- [x] 랜딩이 위젯을 펼친 채로 띄우지 않는다 — 런처만 (DS 5-1)
  - 근거(9/17): `ChatWidget` 의 열림 상태가 `useState(!embedded)` 라, 랜딩(`page.tsx`)이 위젯을 **펼친 채로** 그리고 있었다. 열린 위젯은 480px 이하에서 전체화면(`inset:0`)이라 **375px에서 제품 소개를 통째로 덮었다** — 첫 화면이 소개가 아니라 빈 대화창. `defaultOpen` 프롭으로 드러내고 랜딩은 `defaultOpen={false}`
  - 초점: 사용자가 열지 않았는데 입력창이 초점을 빼앗지 않게 `skipAutoFocus` 로 첫 렌더 1회만 건너뛴다(모바일에서 화면 키보드가 저절로 올라오던 문제). 닫았다 다시 열면 정상적으로 초점이 간다. 데스크톱에서도 랜딩 본문에 Tab 으로 닿지 않던 문제(위젯이 열려 있어 초점이 갇힘)가 함께 풀린다
- [x] 위젯 아이콘을 이모지 → 선 아이콘으로 (DS 5-2)
  - 근거(9/17): 이모지 제거(9/10)가 **랜딩·콘솔에만** 적용돼 있었고 위젯은 💬(런처)·👍👎(평가)·⏳·✓·↗·↑·×·− 가 그대로였다. 런처는 제품에서 가장 많이 노출되는 단일 요소라 여기서 브랜드가 어긋난다. `WIcon`(16px 뷰박스·stroke 1.4)로 랜딩 `Icon`·콘솔 `NavIcon` 과 같은 규약. 이모지는 기기·OS마다 모양이 달라 테넌트 색(`--brand`)도 따라오지 않는다 — 선 아이콘은 `currentColor` 라 이음(#BE5535)에서도 맞는다
  - 엄지 아래는 같은 도형을 180° 돌려 써 모양이 어긋나지 않게. 테스트는 소스가 아니라 **주석을 걷어낸 렌더 구간**만 검사(주석의 화살표로 거짓 실패가 나지 않게)
- [x] 관리 콘솔 화면에서 내부 표기 `[승인 필요]` 제거 (DS 5-3)
  - 근거(9/17): 저장소 안 개발 표기가 운영자 화면 문구 4곳에 그대로 노출돼 있었다(저장 상태 안내·파트너 안내·정산 안내·개인정보 저장 칩). 뜻은 유지하고 문장으로 — 「실제 청구·지급은 계약서가 확정된 뒤에 진행합니다」. 같이 걷어낸 개발자 문구: 「디스크에 쓰지 않습니다」·「파일시스템이 읽기전용」·「백업 API로 내보내」 → 「서버에 보관하지 않습니다」·「「백업 내려받기」로 파일을 받아 두세요」(화면의 실제 버튼 이름과 일치)
  - ⚠️ 승인 자체는 그대로 미승인이다 — 표기만 걷어냈을 뿐 개인정보 저장·LLM 실키·카카오 실연동은 여전히 **[승인 필요]**
- [x] 되돌릴 수 없는 동작을 브랜드 확인 대화상자로 + 실패 삼킴 제거 (DS 5-4)
  - 근거(9/17): 삭제·초기화 4곳이 브라우저 기본 `window.confirm()` 이었다 — 확인 절차 자체는 있었지만(§3 충족) 기본 대화상자는 브랜드를 따르지 않고 주소가 함께 뜬다. `ConfirmDialog`(`role=alertdialog`·`aria-modal`·Tab 순환·ESC 취소·배경 클릭 취소)로 교체하고, **기본 초점은 취소**에 둔다(Enter 로 실수로 지우지 않게). 무엇을 지우는지(자료의 질문·규칙 이름·파트너 이름)를 카드로 따로 강조 — 기본 대화상자에는 없던 것
  - 실패 삼킴: `removeKB`·`resetAll`·`removeCustomRule`·`patchRule` 에 `try/catch` 가 없어 **확인까지 누른 삭제가 오프라인·500에서 조용히 사라졌다**(§3 위반). 네트워크 예외까지 잡아 토스트로 알린다. 폴백 없는 원문 보간(`저장 실패: ${data.error}`) 4곳도 제거 — `error` 가 없으면 화면에 `undefined` 가 떴다. `on401` 도 빠져 있던 3곳에 추가
  - 토큰: 상태 배경 틴트 `--success-50`·`--warn-50`·`--danger-50` 신설(#F0FDF4·#FFFBEB·#FEF2F2 가 화면마다 하드코딩돼 있던 것의 단일 출처). 375px 에서 대화상자 버튼은 full-width 로 쌓이고 최소 높이 40px, `prefers-reduced-motion` 에서 확대 애니메이션 제거
- [x] 위젯 전송 실패 — 답변처럼 보이던 오류 말풍선 + 「다시 보내기」 부재 (DS 5-5)
  - 근거(9/17): 전송이 실패하면 「연결이 원활하지 않아요」가 **평범한 봇 말풍선**(흰 카드·아바타·시각)으로 붙었다 — 사용자는 챗봇이 *그렇게 답했다*고 읽고, 입력창은 이미 비워진 뒤라 **쓴 문장이 사라졌다**(QUALITY_BAR §3 「오류를 삼키고 아무 일 없는 척」의 화면판). 실패 3경로(서버 오류 응답·네트워크 예외·오프라인)를 `Msg.failed` 에 **다시 보낼 사용자 문장**과 함께 담아, 아바타 없는 경고 톤 카드(`--danger-50`/`--danger`·경고 선 아이콘·`role="alert"`)로 그린다. 카드의 「다시 보내기」는 그 안내를 지우고 원문을 그대로 재전송한다(사용자 말풍선은 위에 남아 있으므로 다시 그리지 않는다)
  - 오프라인: `online/offline` 이벤트를 듣고, 끊긴 상태에서는 **요청을 보내기 전에** 막고 같은 카드를 띄운다. 입력창 위에는 경고 톤 배너(`role=status`) — 서버 렌더에서는 상태를 알 수 없어 `false` 로 시작한다(콘솔 DS 4-3 과 같은 방식)
  - 길이: 위젯이 서버(`MAX_MESSAGE_LEN`)와 **같은 2000자**를 알고 있어, 넘으면 보내기 전에 테두리·`aria-invalid`·`role=alert` 문구로 이유를 밝히고 전송을 막는다(종전에는 보낸 뒤 413). 1800자부터 「n / 2000자」 카운터가 나타나고 `aria-describedby` 로 입력과 묶인다. 두 한계값이 어긋나면 테스트가 실패한다
  - 375px: 「다시 보내기」 최소 높이 34px(보조 칩 30px보다 큰 손가락 목표). 하드코딩 색 0건 — 상태 틴트 토큰만
- [x] 본문 바로가기(건너뛰기 링크)·랜드마크 구조 (DS 5-6)
  - 근거(9/17): 키보드·스크린리더 사용자가 본문에 닿기까지 랜딩은 로고+메뉴 6+CTA를, **콘솔은 메뉴 10개+전역 검색+인증 버튼을 매번** Tab 으로 통과해야 했다. 랜딩·약관/방침·콘솔 세 곳에 `.skip-link`(평소 화면 밖, 초점 시 브랜드 pill로 등장 — `display:none` 은 초점을 받지 못한다. `position:fixed` 라 스크롤·사이드바 stacking 과 무관하게 보인다) + 본문 `tabIndex={-1}` 도착 지점
  - 랜딩 랜드마크: `<main>` 이 상단바·푸터·위젯까지 통째로 감싸고 있어 스크린리더의 랜드마크 이동이 아무 데도 데려다주지 못했다 → `main` 은 히어로~CTA 만, 상단바·푸터·위젯은 밖으로
  - 콘솔: 탭을 바꾸면 화면 전체가 바뀌는데 초점은 사이드바 버튼에 남고 바뀐 사실도 알리지 않았다 → 본문으로 초점을 옮긴다(`aria-label={현재 화면 이름}` 이 읽힌다). 첫 렌더에서는 옮기지 않는다
  - 하드코딩 색 2건(`#CBD5E1`·`#94A3B8`) → `--line-2`·`--mut`. 랜딩 TSX 의 hex 0건을 테스트로 고정
  - 덤으로 고친 테스트 결함: `stripComments`(「화면 문자열에 내부 문구 0건」 검사용)의 JSX 주석 패턴이 lazy 매칭으로 **파일 중간을 통째로 삼켜** DS 5-2·5-3 의 「없어야 한다」 검사가 조용히 통과할 수 있었다 — 블록 주석을 먼저 지우고 남은 빈 중괄호를 치우는 순서로 교정
- [x] 약관·방침 공개 페이지의 내부 표기 「[승인 필요]」 제거 (DS 5-7)
  - 근거(9/17): DS 5-3 이 콘솔에서 걷어낸 개발 표기가 **누구나 볼 수 있는** 약관·방침 초안 안내에 그대로 남아 있었다. 뜻은 문장으로(「정식 게시 전 법무 검토를 거치며」). 초안·`[미확정]` 표기는 방문자에게 밝혀야 할 사실이므로 유지 — 테스트가 둘을 함께 고정
  - 승인 자체는 그대로 미승인 — 확정본 반영은 여전히 **[승인 필요]**
- [x] 내려받기 4곳 — 관리 토큰이 주소창·방문 기록에 남고, 실패하면 빈 탭만 뜬다 (DS 5-8)
  - 근거(9/17, 3회차): 「대화 기록」·「백업」·「정산 CSV」·「변경 이력 CSV」가 모두 `window.open('…?token=<관리 토큰>')` 이었다. 토큰이 **주소창·브라우저 방문 기록·서버 접근 로그**에 그대로 남는다 — 콘솔을 잠깐 빌려준 사람도 기록에서 그대로 읽는다(QUALITY_BAR §3 자격 증명 노출). 게다가 401·500 이면 **빈 탭이나 JSON 오류 본문**이 떴고 콘솔에는 아무 안내도 남지 않았다(§1)
  - `downloadFile(url, 무엇, 대체 파일명)` 공통 경로로: 헤더(`x-admin-token`)로 인증해 받고 Blob 으로 저장한다 — 주소에는 아무것도 싣지 않는다. 파일명은 서버의 `Content-Disposition` 을 그대로 쓴다. 실패는 `on401`(→ 잠금 화면)·오류 본문 파싱(실패해도 안내를 잃지 않게 감쌈)·네트워크 예외까지 잡아 토스트로. 서버의 `?token=` 지원은 그대로 둔다(외부 스크립트 호환 — **API 계약 무변경**)
  - 진행 표시: 버튼이 「내려받는 중…」으로 바뀐다. `disabled` 를 쓰지 않는다 — 키보드로 누른 버튼이 그 순간 비활성이 되면 **초점이 본문 밖으로 떨어져** 화면 맨 위부터 Tab 을 다시 눌러야 한다. `busyBtn()`이 `aria-busy`·`aria-disabled`만 달고, 중복 실행은 처리 함수가 앞단에서 막는다. 감사 로그의 CSV `<a href>` 도 버튼으로. 완료·실패는 기존 토스트(`role=status`, 375px 전체폭)가 읽는다
  - 테스트: 렌더된 콘솔 HTML 에 `?token=` 0건 + 소스에 `window.open(` 0건 고정
- [x] 백업 복원 — 되돌릴 수 없는 덮어쓰기에 확인 절차가 없다 (DS 5-9)
  - 근거(9/17, 3회차): 파일을 고르는 **즉시** 지금 등록된 안내 자료·규칙을 통째로 덮어썼다(§3 「되돌릴 수 없는 동작에 확인 절차 없음」). DS 5-4 가 삭제·초기화 4곳을 `ConfirmDialog` 로 옮길 때 복원은 「삭제」로 보이지 않아 빠졌다. `try/catch` 도 없어 오프라인·500 이면 예외가 조용히 사라지고 **복원된 줄 알고 떠나는** 경로가 있었다
  - 같은 확인 대화상자로: 파일명을 카드로 강조하고 「먼저 『백업 내려받기』로 현재 상태를 받아 두세요」를 안내한다(기본 초점은 취소). `on401`·`try/catch`·「복원하는 중…」 표시 추가. JSON 이 아닌 파일은 「백업 파일(JSON) 형식이 아닙니다」로 이유를 밝히고, 실패 안내에 「기존 자료는 그대로입니다」를 덧붙인다
- [x] 남은 쓰기 동작의 실패 삼킴·세션 만료 미처리 (DS 5-10)
  - 근거(9/17, 3회차): DS 5-4 가 삭제·수정 경로만 훑은 탓에 세 곳이 남아 있었다. ① 규칙 **켜기/끄기 스위치**(`toggleCustomRule`) — `try/catch`·`on401` 둘 다 없어, 끊긴 상태에서 스위치를 누르면 되돌아가기만 하고 **왜 안 됐는지 한마디도 없었다**. ② 접수 상태 변경(`patchTicket`)·③ 안내 자료 저장(`submitKB`) — 세션이 만료되면 「저장하지 못했습니다: 관리자 토큰이 필요합니다」라는 내부 문구만 토스트로 떴고 로그인 화면으로 넘어가지 않았다
  - 세 곳 모두 `on401` 을 거치고, 스위치는 성공하면 「『…』 규칙을 켰습니다/껐습니다」로 결과를 알린다. 테스트가 **쓰기 요청 수 ≤ 401 확인 수**를 고정해 새 쓰기 경로가 생겨도 같은 결함이 다시 나지 않게 한다


## 6순위 — 백로그 소진 후 3차 재감사 (DS 6-x)
- [x] 문서 업로드 폼 — 콘솔에 하나 남은 개발자 초안(라벨 없음·오류 삼킴·375px 붕괴) (DS 6-1)
  - 근거(9/18): 콘솔의 다른 폼은 DS 2-4·2-5·2-12 에서 모두 `<label>`+인라인 오류로 올라갔는데 「문서로 안내 자료 만들기」만 **placeholder 4개짜리 초안** 그대로였다. placeholder 는 라벨이 아니다 — 글자를 넣는 순간 사라져 무슨 칸인지 알 수 없고, 스크린리더는 읽어 주지 않는다(6조건 §5). 검증도 토스트 한 줄(「문서명과 본문을 입력해 주세요」)이라 **어느 칸이 비었는지 알려주지 못했다**(§1 「어느 항목이 왜 틀렸는지 인라인으로」)
  - `runImport` 에 **`catch` 가 없었다** — `try/finally` 뿐이라 오프라인·서버 끊김에서 예외가 조용히 사라졌다. 「등록」을 누른 사람은 아무 안내도 못 받고 **등록된 줄 알고 떠났다**(§3 「오류를 삼키고 아무 일 없는 척」). DS 5-4·5-10 이 쓰기 경로를 훑을 때 이 함수만 `flash` 를 이미 쓰고 있어 처리된 것처럼 보여 빠졌다. 실패 안내에 「기존 자료는 그대로입니다」를 붙인다(복원과 같은 규약, DS 5-9)
  - 한계값: 본문은 서버가 10만 자에서 자르고 「한 항목 길이」는 120~2000 밖이면 **말없이 500 으로 바꿔** 버렸다 — 화면은 둘 다 몰랐다. 위젯 2000자(DS 5-5)와 같은 방식으로 옮겨 적고, 넘으면 보내기 전에 이유를 밝힌다. 테스트가 `src/lib/ingest.ts`·`kb/import/route.ts` 의 값과 대조해 어긋나면 실패한다
  - 진행 표시는 `disabled` 가 아니라 `busyBtn()`(DS 5-8) — 키보드로 누른 버튼이 그 순간 비활성이 되면 초점이 본문 밖으로 떨어진다. 어느 버튼이 도는지 구분해 「미리보기 만드는 중…」·「등록하는 중…」. 중복 실행은 `runImport` 앞단이 막는다
  - 375px: `2fr 1fr 1fr` 고정 3열이 그대로 눌려 숫자 칸이 손가락보다 좁아졌다 → `.ac-grid3` 로 옮겨 ≤900px 에서 한 칸씩 쌓는다. 오류가 있으면 **첫 오류 칸으로 초점**을 옮긴다(화면 밖 칸이 틀렸을 수 있다)
- [x] 상태 색 28곳이 토큰을 우회한다 — 「단일 출처」가 이름뿐이었다 (DS 6-2)
  - 근거(9/18): DS 5-4 가 `--success-50`·`--warn-50`·`--danger-50` 을 globals.css 에 「화면마다 하드코딩돼 있던 것의 단일 출처」로 세웠지만, 정작 **콘솔 화면에는 같은 틴트 hex 가 25곳 그대로 남았다**(테두리·코드 블록까지 28곳) — 토큰을 바꿔도 따라오지 않는 색들이다. `TICKET_STATUS_TONE` 바로 위 주석은 「토큰만 쓴다」였는데 그 아래 첫 줄이 `#FFFBEB` 였다. 랜딩(DS 5-6)·위젯(DS 5-2)은 이미 hex 0건이라 콘솔만 어긋나 있었다
  - `TONE`(ok·warn·danger·brand·mute) 한 곳으로 모으고 화면은 `style={TONE.warn}` 만 참조한다. 테두리용 `--warn-200`(#FDE68A)·`--danger-200`(#FECACA) 은 토큰이 아예 없어 새로 세웠다(틴트 배경보다 한 단 진한 짝). 어두운 배경 위 코드 블록의 `#E2E8F0` → `var(--line)`
  - 테스트가 콘솔 TSX 의 hex **0건**을 고정한다(랜딩과 같은 규칙). 「AICC Portal 과 같은 색인가」를 이제 한 파일에서 판단할 수 있다
- [x] 탭이 주소에 남지 않아 새로고침·뒤로가기·링크 공유에서 자리를 잃는다 (DS 6-3)
  - 근거(9/18): 탭 10개가 전부 `useState` 뿐이라 ① **새로고침하면 어디에 있었든 대시보드**로 돌아갔고(정산 리포트를 보다 F5 를 누르면 조건·기준월까지 통째로 날아간다) ② **뒤로가기는 이전 탭이 아니라 콘솔 자체를 벗어났다**(QUALITY_BAR §1 「새로고침·뒤로가기에도 상태가 깨지지 않는다」) ③ 「정산 화면 좀 봐주세요」라며 보낼 링크가 없었다
  - 주소 뒤 `#settle` 로 남긴다. 해시는 서버로 가지 않으므로 서버 렌더는 `'dash'` 고정(hydration 불일치 방지)이고 마운트 후 주소에 맞춘다. 탭 이름은 `TAB_GROUPS` 에서 뽑아 **메뉴와 같은 출처** — 모르는 이름이면 대시보드로 되돌린다
  - 전환은 모두 `goTab()` 한 문을 지나 `pushState` 로 방문 기록에 남기고(`pushState` 는 `hashchange` 를 일으키지 않아 되먹임이 없다), 뒤로/앞으로는 `hashchange` 로 되받는다. 주소를 바꾸지 못하는 환경에서도 화면 전환 자체는 막지 않는다
  - 초점: 뒤로/앞으로는 사용자의 이동이므로 본문으로 초점을 옮기지만(DS 5-6 규약), **깊은 링크로 막 들어온 첫 진입에서는 가로채지 않는다** — 사용자가 탭을 바꾼 것이 아니기 때문이다. 탭 키와 겹치는 요소 `id` 가 없음을 확인(브라우저가 엉뚱한 곳으로 스크롤하지 않게)


## 7순위 — 백로그 소진 후 4차 재감사 (DS 7-x)
- [x] 위젯 대화가 새로고침·페이지 이동마다 처음으로 돌아간다 — 내건 차별점이 실제로는 안 됐다 (DS 7-1)
  - 근거(9/18): `sessionId` 가 `useState(() => web_…)` 라 **마운트마다 새로 생겼고** 말풍선도 인사말로 초기화됐다. 임베드 위젯은 호스트가 페이지를 옮길 때마다(목록 → 상세 → 장바구니) iframe 이 통째로 다시 로드된다 — **링크 한 번 누를 때마다 대화가 처음부터**다. 멀티턴 접수(`lib/slots.ts` 예약·장애 신고)를 절반 진행하다 새로고침하면 서버 세션(TTL 30분)은 멀쩡히 살아 있는데 클라이언트가 새 세션을 만들어 **같은 것을 처음부터 다시 묻는다**. QUALITY_BAR §1 「새로고침·뒤로가기에도 상태가 깨지지 않는다」 위반이자, §2 차별점으로 내건 「끊김 없는 연결 — 화면이 바뀌어도 맥락이 유지된다」가 화면에서는 반대로 동작하고 있었다
  - `sessionStorage` 에 `{v, id, at, msgs}` 로 저장하고 마운트 후 복원한다(서버 렌더에는 저장소가 없으므로 hydration 이후 — 콘솔 DS 4-3·위젯 DS 5-5 와 같은 방식). **`localStorage` 를 쓰지 않는다**: 탭을 닫아도 남아 공용 PC 에서 다음 사람이 읽는다. 저장 범위도 위젯 출처(iframe) 안이라 호스트 페이지는 읽지 못한다. 열쇠는 테넌트별로 나눠 다른 안내 챗봇의 대화가 섞이지 않게 한다
  - 복원 창은 **서버 세션 TTL 과 같은 30분**(`THREAD_TTL_MS` ↔ `lib/session.ts` `TTL_MS`) — 서버 문맥이 이미 지워졌는데 화면만 이어 보이면 「아까 말한 그거요」가 통하지 않는다. 두 값이 어긋나면 테스트가 실패한다(위젯 2000자 ↔ 서버 `MAX_MESSAGE_LEN`, DS 5-5 와 같은 규약). 만료분은 읽는 김에 지운다
  - 저장하지 않는 것: **전송 실패 안내**(그때의 상황이다 — 다시 열었을 때 남아 있으면 지나간 오류를 현재로 읽는다), 연락처 입력 카드·접수 진행 상태(개인정보이고, 복원하면 중복 접수가 된다), 평가 상태. 인사말 하나뿐이면 저장하지 않고 지운다. 상한은 말풍선 40개·직렬화 100KB(호스트 저장소를 가득 채우지 않게)
  - 실패해도 대화는 계속된다: 서드파티 쿠키를 막은 브라우저의 iframe·사생활 보호 모드에서는 **저장소 접근 자체가 예외를 던진다**(임베드에서 드문 일이 아니다) — `threadStore()`·읽기·쓰기·지우기를 모두 감싼다. 깨진 JSON·남의 형식은 새 대화로 시작한다(사용자에게 알릴 실패가 아니라 기능 저하다)
  - 「닫고 처음으로」는 저장분까지 지우고 **새 세션으로** 간다 — 사용자가 명시적으로 지운 대화가 서버 문맥으로 이어지면 안 된다. 되살린 경우에는 목록 맨 위에 「이전 대화를 이어서 보고 있습니다」를 밝힌다(설명 없이 지난 말풍선이 있으면 혼란스럽다). 빠른 답장 칩은 이어가는 대화에서는 뜨지 않는다(`msgs.length === 1` 조건 그대로)
  - 테스트: 저장소를 갈아 끼워 **왕복**을 검사한다(정상 복원·테넌트 분리·만료 폐기·실패 안내 제외·인사말뿐일 때 미저장) + 실패 경로 4종(접근 차단·쓰기 실패·깨진 JSON·저장소 없음)에서 예외가 새지 않는 것 + 서버 렌더에 이어가기 표시 0건. `tests/widget.test.mjs` DS 7-1
- [x] 비모달 위젯이 키보드 초점을 가둔다 — 스크린리더에 알린 것과 실제 동작이 달랐다 (DS 7-2)
  - 근거(9/18): `aria-modal` 은 **전체화면일 때만** 붙는데 `onPanelKeyDown` 의 Tab 순환은 **항상** 돌았다. 데스크톱에서 위젯을 열면 배경 가림막도 없고 페이지도 그대로 읽히는데, 키보드 사용자만 위젯 안에 갇혀 본문으로 돌아가지 못했다 — 위젯을 열어 둔 채 페이지를 계속 보는 것이 채팅 위젯의 정상 사용이다. 스크린리더에는 「배경도 쓸 수 있다」(`aria-modal` 없음)고 알려 놓고 키보드는 가두는 **불일치**이기도 하다
  - 가두기는 `open && mobile`(= `fullscreen` = `aria-modal`) 일 때만. ESC 로 닫는 경로는 두 경우 모두 그대로다. 검사 순서(Tab → 모달 여부 → 초점 목록)를 테스트가 고정한다. 임베드 모드는 iframe 경계가 자연스러운 경계라 영향이 없다
- [x] 전체화면 위젯 뒤에서 호스트 페이지가 따라 움직인다 (DS 7-3)
  - 근거(9/18): 모바일에서 위젯은 `inset:0` 전체화면인데 **뒤 페이지 스크롤을 잠그지 않았다** — 대화 목록 끝에서 스크롤을 이어가면(스크롤 체이닝) 뒤 화면이 밀리고, 위젯을 닫으면 읽던 자리가 아니다. 임베드 모드는 iframe 이 화면 전체를 덮어 **호스트 문서**가 밀리므로 위젯 안에서는 손댈 수 없다
  - 랜딩(비임베드)은 위젯이 `document.body` 를 잠그고 닫을 때 **원래 값**을 되돌린다(호스트가 자기 스타일로 `overflow` 를 쓰고 있을 수 있다). 임베드는 `embed.js`(v0.6)가 `fullscreen` 신호에 맞춰 같은 일을 하고, 풀 때 **읽던 스크롤 위치까지** 되돌린다. 위젯을 걷어내는 `remove()` 에서도 잠금을 푼다(잠긴 페이지가 남지 않게)
  - 대화 목록에 `overscroll-behavior: contain` — 목록 끝에서 스크롤이 뒤로 새지 않는다. `data-tenant`·`data-position` 옵션 계약은 그대로임을 테스트가 함께 고정한다

## 8순위 — 백로그 소진 후 5차 재감사 (DS 8-x)
- [x] 동작 버튼 20곳이 누르는 순간 키보드 초점을 떨어뜨린다 — 서랍에서는 초점 가두기까지 새어 나갔다 (DS 8-1)
  - 근거(9/18): DS 5-8 이 내려받기 버튼을 옮기며 「`disabled` 를 쓰지 않는다 — 키보드로 누른 버튼이 그 순간 비활성이 되면 **초점이 본문 밖으로 떨어져** 화면 맨 위부터 Tab 을 다시 눌러야 한다」고 이유까지 적어 두고 `busyBtn()` 을 세웠는데, 정작 **나머지 동작 버튼 16곳은 그대로였다**(로그인·안내 자료 저장·규칙 저장·접수 상태 변경 2종·파트너/고객사 저장 2종·정산 다시 계산·CSV·각 탭 새로고침 4종·응답 테스트). DS 6-2 의 「토큰이 이름뿐이었다」와 같은 종류 — 규약만 있고 화면은 따라오지 않았다
  - ⚠️ **접수 상세 서랍(`role=dialog`)이 가장 나빴다**: 초점 가두기가 `filter(el => !el.hasAttribute('disabled'))` 로 목록을 만드는데, 「상담 시작」을 키보드로 누르면 그 버튼이 비활성이 되어 `document.activeElement` 가 `<body>` 가 된다 — 순환의 기준점이 사라지므로 다음 Tab 은 **서랍 밖 뒤 페이지로 새어 나간다**. 모달이라고 알려 놓고 가두지 못한다
  - 위젯(DS 8-2)과 같은 원칙: 버튼은 계속 초점을 받고(`aria-busy`·`aria-disabled`), **중복 실행은 처리 함수 앞단이 막는다**. `useRunOnce()`(claim/release)를 세워 7개 경로(로그인·규칙 저장·접수 상태·파트너/정산/테넌트/저장소 불러오기)에 붙였다 — 진행 중 state(`...Busy`)가 아니라 **ref** 다: 같은 틱에 두 번 눌리면 state 는 아직 바뀌지 않아 두 번째가 새어 나간다. 테스트가 claim 수 = release 수를 고정한다(집고 놓지 않으면 그 버튼은 영영 동작하지 않는다)
  - 정산 CSV 는 「내려받을 것이 없음」도 `disabled` 였다 → 잠긴 모양은 그대로 두되 눌리면 **왜** 없는지 밝힌다(「기준월을 바꾸거나 『다시 계산』을 눌러 주세요」). 테스트: 렌더된 콘솔 HTML 에 `<button … disabled>` 0건 + 첫 렌더에 `aria-disabled` 0건(아무것도 누르지 않았는데 잠겨 보이면 그 자체가 결함)
- [x] 위젯 「접수하기」가 이유 없이 잠긴다 — 무엇이 틀렸는지 한마디도 없었다 (DS 8-2)
  - 근거(9/18): 상담원 연결 카드의 버튼이 `disabled={!validContact(contact)}` 였다. 「010-1234」를 넣은 사람에게 화면이 주는 정보는 **흐려진 버튼 하나**다 — 자릿수가 문제인지, 이메일만 되는지, 접수 자체가 막힌 건지 알 수 없다. Enter 도 `validContact` 조건에 막혀 조용히 삼켜졌다. QUALITY_BAR §1 「잘못된 입력에 어느 항목이 왜 틀렸는지 인라인으로 알려준다」 위반이고, 콘솔 폼은 DS 2-4·2-5·2-12·6-1 에서 전부 인라인 오류로 올라갔는데 **고객이 쓰는 화면만** 남아 있었다
  - `contactError()` 가 사유를 나눠 돌려준다(비었을 때는 「연락처 없이 접수」라는 길까지·이메일 형식·전화번호 자릿수·형식 불명·너무 김). 오류는 `role=alert` + `aria-invalid` + `aria-describedby` 로 칸과 묶고, **틀린 칸으로 초점을 돌려준다**(스크롤 밖일 수 있다). `contactError()==='' ↔ validContact()` 가 어긋나면 「눌러도 안 되는데 오류도 없는」 상태가 되므로 테스트가 왕복으로 고정한다
  - 보내는 중 입력 칸은 `disabled` 가 아니라 **`readOnly`** — 비활성은 그 칸에 있던 초점을 떨어뜨린다. 위젯의 나머지 버튼 9곳(전송·빠른 답장·추천 질문·다시 보내기·이전/건너뛰기/취소·연락처 없이 접수·취소)도 `aria-disabled` 로 바꾸고 모양만 잠근다(`chip()`) — 실제 차단은 `sendText()`·`submitHandoff()` 앞단이 이미 하고 있었다
  - 375px·스크린리더: 오류 문구는 `lineHeight 1.5` 로 두 줄까지 접히고, 「연락처 없이 접수」는 형식 검사를 타지 않는다(`skipContact`). 렌더된 위젯 HTML 에 `<button … disabled>` 0건을 테스트가 고정
- [x] 모션 최소화 설정을 켜 두어도 화면이 미끄러진다 — CSS 로 막은 것을 JS 가 도로 켰다 (DS 8-3)
  - 근거(9/18): globals.css 에 `@media (prefers-reduced-motion: reduce){html{scroll-behavior:auto}}` 가 있고 애니메이션 5종도 꺼 두었는데, **JS 가 `scrollIntoView({behavior:'smooth'})` 를 직접 넘기는 5곳은 그 설정을 무시한다**(인자가 CSS 보다 우선한다). 전정 장애가 있는 사용자에게는 이 미끄러짐 자체가 증상을 일으킨다 — 끄라고 만든 스위치가 켜져 있는데도 동작하지 않았다
  - 가장 잦은 곳이 **위젯 대화 자동 스크롤**이다: 말풍선이 붙을 때마다, 답을 기다릴 때마다 돈다(`[msgs, busy, open]`). 콘솔 3곳(안내 자료·규칙·파트너 「수정」 → 폼으로 이동)도 같다
  - `scrollBehavior()` 로 설정을 읽어 `'auto'|'smooth'` 를 고른다. `matchMedia` 가 없는 환경에서 예외로 죽으면 **스크롤 자체가 멈추므로** 감싼다. 테스트가 두 파일의 `behavior: 'smooth'` 0건 + 폼 이동 3곳 전부 적용을 고정

## 9순위 — 백로그 소진 후 6차 재감사 (DS 9-x)
- [x] 표 9곳 중 8곳이 375px 에서 화면 밖으로 밀린다 — 가려진 칸은 키보드로 영영 볼 수 없었다 (DS 9-1)
  - 근거(9/19): 콘솔 표 9개 중 가로 넘침을 받아 주는 상자가 있는 곳은 **지식베이스 하나뿐**이었다(`<div style={{overflowX:'auto'}}>`). `.ac-col-wide` 가 ≤900px 에서 넓은 열을 접어 주지만 남는 칸에도 줄일 수 없는 최소 폭이 있다 — 접수 표는 375px 본문 폭 343px 에 접수(80)+상태(80)+메시지(minWidth 160+여백)+처리 버튼(95) ≈ 435px 라 **카드를 밀어내 화면 전체가 가로로 흐른다**(고객사·파트너·감사 로그·정산 근거도 같다). 상단바·사이드바가 어긋나고 오른쪽 끝 동작 버튼이 화면 밖으로 나간다
  - 더 나쁜 쪽은 **하나 있던 그 상자**였다: `overflow-x:auto` 만 있고 `tabindex` 가 없었다. 스크롤 영역 안에 초점 받을 것이 없는 칸(키워드·날짜·금액)만 가려져 있으면 마우스·터치가 없는 사용자는 그 칸을 볼 방법이 없다(WCAG 2.1.1). 설치 탭의 **설치 코드 블록**(`<pre overflowX:auto>`)도 같다 — 한 줄짜리 스니펫이라 좁은 화면에서는 반드시 넘치는데 끝까지 읽을 수 없었다
  - 공통 껍데기 `ScrollX`(`.ac-scrollx`) 하나로 모으고 표 9곳·코드 블록에 붙였다. `role=region` + `tabIndex` + 「가로로 스크롤할 수 있습니다」 이름은 **실제로 넘칠 때만** 붙인다(넘치지 않는데 Tab 이 멈추면 그 자체가 방해다) — `scrollWidth - clientWidth` 를 `ResizeObserver` 로 재고, 없는 환경은 `resize` 로 따라간다. 서버 렌더·측정 전에는 닿을 수 있는 쪽이 기본값이다(JS 가 죽어도 스크롤은 남는다). 초점 테두리(`:focus-visible`)를 반드시 보인다
  - 테스트가 `<table className="ac-table">` **바로 앞 줄이 `<ScrollX label=`** 임을 9곳 모두에서 고정하고, 화면이 `overflowX:'auto'` 를 직접 쓰는 것을 0건으로 막는다(DS 6-2 「단일 출처」와 같은 규칙) — 새 표가 생겨도 같은 결함이 다시 나지 않는다
- [x] 설치 「선택 옵션」 표만 공통 표 규격을 우회했다 (DS 9-2)
  - 근거(9/19): 콘솔의 표는 모두 `.ac-table` 인데 이 표만 `borderCollapse`·`padding: '8px 6px'`·`borderBottom: '1px solid var(--line)'` 을 **칸마다 6번 적어 둔** 손수 만든 표였다. 표 규격을 손보면 이 화면만 어긋난다(DS 6-2 와 같은 종류)
  - `scope="col"` 이 없어 스크린리더가 「옵션」·「설명」을 열 머리로 읽지 못했다(다른 표는 DS 2-x 에서 모두 붙였다). `.ac-table` + `scope` 로 옮기고 옵션 이름은 `nowrap`(`data-tenant="eum"` 이 줄바꿈되면 읽을 수 없다), 표 자체는 DS 9-1 의 스크롤 영역 안에 넣었다. 테스트가 콘솔 TSX 의 `borderCollapse` **0건**을 고정한다
- [x] 정산 기준월·파트너 조건이 주소에 남지 않는다 — DS 6-3 이 적어 둔 문제의 남은 절반 (DS 9-3)
  - 근거(9/19): DS 6-3 은 「정산 리포트를 보다 F5 를 누르면 **조건·기준월까지** 통째로 날아간다」를 근거로 세워 놓고 탭만 주소에 남겼다. 그래서 ① 8월 정산을 보다 새로고침하면 **이번 달**로 돌아가고 ② 「정산 화면 좀 봐주세요」라며 보낸 `#settle` 링크는 받는 사람에게 언제나 이번 달로 열린다 — 보낸 사람이 본 화면이 아니다(QUALITY_BAR §1)
  - `#settle?m=2026-08&p=<파트너>` 로 남긴다. 탭 이름과 조건을 `viewFromHash()` 한 파서에서 읽어 `tabFromHash()` 도 같은 출처를 쓴다. **주소에서 온 값은 믿지 않는다** — `MONTH_RE` 를 통과하지 못하면(`2026-13`·`2026-9`) 보던 달을 지키고, 파트너 식별자는 64자에서 자른다
  - 조건이 달라지면 이전 달의 표를 화면에 그대로 두지 않는다(`setSettleReport(null)`) — 비우면 탭 진입 로더가 새 조건으로 다시 계산한다. 기준월 변경은 **방문 기록을 쌓지 않는다**(`replaceState`): 달을 다섯 번 바꾼 뒤 뒤로가기가 다섯 번 필요하면 「뒤로는 이전 탭」이라는 DS 6-3 의 약속이 깨진다. 메뉴로 정산 탭에 들어와도 조건이 곧바로 주소에 실린다
  - 주소를 바꾸지 못하는 환경에서도 계산 자체는 막지 않는다(`try/catch`, DS 6-3 과 같은 규약). 테스트가 `MONTH_RE` 를 실제로 돌려 경계값 9종을 확인하고, 조건 경로에 `pushState` 가 0건임을 고정한다

## 10순위 — 백로그 소진 후 7차 재감사 (DS 10-x)
- [x] 보조 문구 색 토큰이 WCAG AA 에 2.5:1 로 미달했다 — 94곳이 같은 값을 참조한다 (DS 10-1)
  - 근거(9/19): `--mut:#94A3B8` 은 흰 카드 위에서 **2.56:1**, 페이지 배경(`--bg`) 위에서 **2.45:1** 이다. AA 본문 기준 4.5:1 의 절반이고, 큰 글자 완화 기준 3:1 조차 넘지 못한다. 그런데 이 토큰이 쓰이는 자리는 하나같이 **작고 설명하는 글자**다 — 표의 열 머리(11.5px), 「측정 중」 KPI 값, 말풍선 시각, 규칙 힌트, 빈 상태 안내, 글자 수 세기, 그리고 위젯 하단 **AI 고지**(10.5px, 「AI 자동응답 · 등록된 안내 자료 기반」). 고지는 읽히라고 붙인 문장인데 가장 읽기 어려운 색이었다. 소스 94곳이 `var(--mut)` 을 참조하므로 **화면을 고칠 일이 아니라 토큰 하나를 고칠 일**이다(DS 6-2 가 세운 단일 출처가 여기서 값을 한다)
  - 밝은 면은 흰 카드만이 아니다: 편집 중 행·최신 근거 카드는 `--brand-50`, 상태 배너는 `--success-50`·`--warn-50`·`--danger-50` 이 깔린다. slate-500(#64748B)은 흰 배경에서는 통과하지만 **`--brand-50`·`--danger-50` 위에서 4.37:1 로 여전히 미달**이라 한 단 더 내린 **#5F6E85** 로 잡았다 — 6개 면 전부에서 최소 4.73:1. 위계는 유지한다(`--sub` 7.58:1 › `--mut` 5.18:1)
  - 어두운 면에서는 **부호가 뒤집힌다**: 밝은 면을 통과시키려 어둡게 잡은 색은 `--ink` 카드 위에서 3.75:1 로 도로 미달한다. 짝 토큰 `--mut-on-ink:#94A3B8`(ink 위 6.96:1)을 세우고, 어두운 배경을 쓰는 유일한 자리인 랜딩 최종 CTA 잔글씨를 그리로 옮겼다. 파랑(`--brand`) 배경 위에는 `--mut` 를 쓰는 곳이 없음을 확인했다(모두 `#fff`)
  - 테스트가 눈대중을 대신한다: `globals.css` 의 `:root` 를 **실제로 파싱해** WCAG 상대 휘도를 계산하고 `--sub`·`--mut` × 밝은 면 6종 = 12쌍이 4.5:1 을 넘는지 확인한다. `--mut` 가 어두운 면에서 미달임(=짝 토큰이 필요함)과 위계(`--mut` < `--sub`)도 함께 고정해, 나중에 누가 값을 되돌리면 이유까지 같이 깨지게 했다
- [x] 랜딩 앵커 7곳이 스티키 상단바 아래로 착지한다 — 법무 페이지만 이 문제를 풀어 두었다 (DS 10-2)
  - 근거(9/19): 상단바는 `position:sticky`·62px(+테두리 63px)인데 `#trust`·`#features`·`#channels`·`#steps`·`#faq`(메뉴 5개)와 `#demo`·`#contact`(히어로 CTA 2개)가 가리키는 섹션에는 여백 지정이 없었다. 메뉴를 누르면 섹션의 눈표(「동작 방식」)와 제목 첫 줄이 상단바 **뒤로 들어간다** — 누른 사람이 찾던 바로 그 제목이다. 정작 `.legal-body h2` 에는 `scroll-margin-top:84px` 가 이미 있었다(DS 4-4). 같은 셸을 쓰면서 한쪽만 풀어 둔 것으로, DS 6-2·9-2 와 같은 종류의 어긋남이다
  - 화면마다 `scroll-margin` 을 적는 대신 `html{scroll-padding-top:78px}` **한 곳**으로 세웠다(스크롤 컨테이너 쪽 속성이라 앵커·`scrollIntoView`·초점 이동에 모두 걸린다). 법무 페이지의 `scroll-margin-top` 은 **지웠다** — 둘은 더해지므로 남겨 두면 착지점이 162px 아래로 내려간다. 78px 은 상단바 63px + 여유 15px
  - 테스트가 ① `scroll-padding-top` 이 상단바 높이(63px) 이상인지 ② `scroll-margin-top` 이 CSS 전체에 0건인지(단일 출처가 다시 갈라지지 않게) ③ 랜딩의 `href="#…"` 이 가리키는 **id 가 실제로 존재**하는지(죽은 앵커도 같이 막는다)를 고정한다. 기존 DS 4-4 테스트의 단언도 새 출처를 보도록 옮겼다
  - 확인(9/19, 라이브 375px): 계산된 `scroll-padding-top` 78px · 상단바 63px · 가로 넘침 0px
- [x] 이름을 붙여 둔 상자 4곳이 보조기술에 전달되지 않는다 — `aria-label` 을 붙일 수 없는 요소였다 (DS 10-3)
  - 근거(9/19): `<div>` 의 기본 역할은 `generic` 이고, **generic 은 작성자 이름(`aria-label`·`aria-labelledby`)을 받지 않는다**(ARIA 규격). 즉 적어 둔 문장이 코드에만 남고 화면에는 없는 것과 같다. 랜딩 히어로의 **대화 예시 카드**(`aria-label="상담 화면 예시"`)가 그랬다 — 스크린리더 사용자에게는 「환불은 며칠까지 가능한가요? / 수령일로부터 7일 이내…」가 **예시라는 표시 없이 본문으로** 읽히고, 그것이 예시라는 안내는 세 말풍선을 다 읽은 **뒤에야** 나온다. §13 「근거 없는 수치를 쓰지 않는다」와 같은 취지의 문제다: 화면은 예시라고 밝히는데 보조기술에는 밝히지 않았다
  - 콘솔에도 같은 것이 3곳 있었고 **하나는 안내를 통째로 삼켰다**: 응답 테스트 미리보기의 타이핑 표시(`<div className="ac-pv-bot" aria-label="답변을 작성하고 있습니다">`)는 안의 점 3개가 빈 `<span>` 이라 읽을 내용이 없다 — 이름이 무시되면 **아무 소리도 나지 않는다**. 답을 기다리는 동안 화면은 점이 뛰는데 듣는 사람에게는 정적이다. 정작 위젯(ChatWidget)은 같은 표시에 `role="status"` 를 붙여 두었다 — 규약은 있는데 콘솔만 따라오지 않은, DS 8-1·6-2 와 같은 모양이다
  - 히어로는 `<figure>` + `<figcaption>` 으로 옮겼다(figure 는 이름을 받는 역할이고, 들어서는 순간 「예시」가 먼저 전달된다). 안내문은 figure 의 직계 `figcaption` 이라 프로그램적으로도 묶인다. 콘솔 3곳은 같은 파일이 이미 쓰던 규격을 그대로 썼다 — 칩 묶음·미리보기 묶음은 `role="group"`, 타이핑 표시는 **위젯과 같은** `role="status"`
  - 테스트가 세 화면(랜딩·콘솔·위젯)의 `<div … aria-label|aria-labelledby>` 를 전부 훑어 **`role` 이 없으면 실패**시킨다 — 새 화면이 같은 실수를 반복할 수 없다. `<ul aria-label>`(최근 대화 목록)은 `list` 역할이라 그대로 둔다
  - 확인(9/19, 라이브): `<figure>` 의 접근 이름이 「상담 화면 예시 — 실제 답변은 등록한 자료에 따라 달라집니다」로 노출되고, `figcaption` 이 직계 자식으로 붙었다

## 11순위 — 백로그 소진 후 8차 재감사 (DS 11-x)
- [x] 상태색 세 개가 WCAG AA 에 미달했다 — DS 10-1 이 보조 문구만 고치고 상태색은 지나쳤다 (DS 11-1)
  - 근거(9/19): `--success:#16A34A` 는 흰 카드 위 **3.30:1**, 자기 틴트(`--success-50`) 위 **3.15:1**, 가장 나쁜 면(`--danger-50`)에서 **3.01:1** 이다. `--warn:#D97706` 은 **3.19 / 3.04 / 2.91:1** 로 더 낮아 **큰 글자 완화 기준 3:1 조차 넘지 못한다**. `--danger:#DC2626` 은 흰 배경(4.83:1)만 보면 통과하지만 **자기 틴트 위에서 4.41:1 로 미달**인데, 하필 그 조합이 실제 화면의 오류 배너다(콘솔 파트너 오류 `danger-50` 배경 + `--danger` 글자, 위젯 전송 실패 말풍선도 같다)
  - 이 색들이 쓰이는 자리는 전부 **작고 상태를 알리는 글자**다: 상담원 요청 큐의 처리 상태 pill(`TONE.ok`·`TONE.warn`, 콘솔에서 26곳이 참조), 저장소 상태 배지(`.ac-badge[data-tone]`), 오프라인 배너, 약관·개인정보 페이지의 **「검토 초안」 고지**(`.legal-notice` 13.5px·`.lg-pill`·`.todo`), 필수 입력의 빨간 `*`. 「지금 어떤 상태인가」를 알리려고 색을 쓴 자리가 가장 읽기 어려웠다 — DS 10-1 과 같은 종류의 결함을 **같은 파일 네 줄 아래**에 남겨 둔 셈이다
  - 밝은 면 6종(흰 카드·페이지 배경·`--brand-50`·상태 틴트 3종) 전부에서 4.5:1 을 넘는 값으로 한 단씩 내렸다: `--success:#15803D` · `--warn:#B45309` · `--danger:#C42121` — **최소 4.59 / 4.59 / 5.35:1**. 어두운 면 위에는 상태색을 쓰는 자리가 없음을 확인했다(랜딩 최종 CTA 는 `--mut-on-ink` 만 쓴다)
  - 상태색은 **채워진 면**으로도 쓰인다 — 삭제 확인 버튼 배경(흰 글자)·스위치 켜짐 배경(흰 손잡이)·대시보드 차트의 「상담원 제안」 막대. 어두워지면서 비텍스트 대비(WCAG 1.4.11, 3:1)도 같이 올라간다(스위치 3.30 → 5.02:1). 테스트가 `:root` 를 실제로 파싱해 3색 × 밝은 면 6종 = 18쌍과 「면 위 흰 글자」 3쌍을 함께 고정한다
- [x] 상태 틴트가 토큰을 우회해 화면에 18번 박혀 있었다 (DS 11-2)
  - 근거(9/19): `--warn-50:#FFFBEB`·`--warn-200:#FDE68A` 를 `:root` 에 세워 두고도, 같은 파일 본문 9개 규칙이 **같은 hex 를 다시 적어** 쓰고 있었다(`.ac-badge`·`.ac-linkbtn:hover`·`.ac-offline`·`.ac-nscard` 2종·`.lg-pill`·`.legal-notice`·`.todo`). DS 11-1 로 `--warn` 만 내리면 **글자는 진해지는데 배경·테두리는 옛 값 그대로** 남아 화면마다 톤이 갈라진다 — DS 6-2·9-2 와 같은 종류다
  - 성공 계열은 테두리 토큰 자체가 없어서 `#BBF7D0` 이 규칙 안에 박혀 있었다(짝이 `--warn-200`·`--danger-200` 만 있었다). `--success-200` 을 세워 세 계열의 짝을 갖췄다. 오프라인 배너는 글자색까지 하드코딩(`#92400E`)이라 토큰을 바꿔도 따라오지 않았다 — `var(--warn)` 로 옮겼다(그 배경 위 4.84:1)
  - 테스트가 `:root` **바깥**의 색 리터럴을 0건으로 막는다(`#fff` 만 예외 — 중립색이라 토큰이 아니다). 기존 DS 4-3 테스트가 보던 단언(`.ac-offline` 에 `#FFFBEB`)도 새 출처를 보도록 옮겼다
- [x] 새 창으로 열리는 링크 3곳이 그 사실을 알리지 않았다 (DS 11-3)
  - 근거(9/19): `target="_blank"` 는 세 곳이다 — 위젯 답변의 **신청 CTA**(`m.cta`), 콘솔 테넌트 탭의 **신청 버튼 주소**, 설치 탭의 **「동작 화면 보기」**. 위젯은 고객사 사이트 위 **iframe 안**이라 새 탭이 떠도 창 테두리가 보이지 않는다 — 「신청하기」를 누른 사람은 상담창이 사라진 줄 알고 뒤로가기를 찾는다(위젯에는 없다). 스크린리더는 「신청하기, 링크」까지만 읽고 창이 바뀐다는 말은 하지 않는다(WCAG G201)
  - 위젯에는 이미 external 표시 아이콘이 있었지만 `aria-hidden` 이라 **보는 사람에게만** 전달됐다. 콘솔 두 곳은 표시조차 없었다
  - 접근 이름 뒤에 「— 새 창에서 열립니다」를 붙였다. **보이는 글자로 시작**하게 두어 음성 입력이 이름으로 누를 수 있게 한다(WCAG 2.5.3 Label in Name). 콘솔은 링크마다 적지 않고 공통 껍데기 `ExternalLink` 하나에서만 만든다 — 표시(아이콘)와 고지(이름)가 갈라지지 않게 한 출처에 둔다(DS 9-1 의 `ScrollX` 와 같은 방식)
  - 테스트가 세 화면에서 `target="_blank"` 수와 고지 수가 **같은지** 확인하고(새 링크가 생기면 바로 깨진다), 콘솔은 `ExternalLink` 를 거치지 않는 새 창 링크가 0건임을, 접근 이름이 보이는 글자로 시작함을 고정한다. `rel="noopener noreferrer"` 규약도 함께 본다
  - 375px·키보드: 「동작 화면 보기」가 든 줄은 `flex-wrap` 이라 좁은 화면에서 두 줄로 접힌다. 주소 칸은 `word-break:break-all` 로 접히고 12px 아이콘이 마지막 줄에 따라붙는다. 아이콘은 `aria-hidden` 이라 이름을 두 번 읽지 않는다

## 완료 기준
QUALITY_BAR 6조건 + **"AICC Portal 옆에 두었을 때 같은 회사 제품으로 보이는가"**. 각 항목은 375px·키보드·스크린리더 확인 후 `[x]`.
