# 챗봇 디자인 스프린트 — AICC Portal 수준으로 (최우선 백로그, 9/9~9/16)

> 진단(9/9): 기능·테스트는 충분(156건)하나 화면이 개발자 초안 수준. 콘솔에 내부 문구 노출, 대시보드는 텍스트만, 랜딩은 히어로 하나.
> 목표: **AICC Portal(callbot-portal)과 같은 라인의 제품처럼 보일 것.** 디자인 시스템을 이식한다 — 새로 만들지 않는다.

> 진행(9/10): 디자인 토큰을 `src/app/globals.css` 로 이식 완료 — 화면은 모두 var() 참조라 랜딩·콘솔·위젯에 동시에 반영된다.
> 진행(9/10, 2회차): 1순위 완료(상담원 전환 카드·임베드 깜빡임). 2순위는 셸·대시보드까지 완료.
> 진행(9/10, 3회차): 최근 대화 상세 서랍·지식베이스 표/편집 폼·응답 테스트 분할 완료. 남은 것은 룰 카드 빌더·로그인 화면.
> 진행(9/14): 룰 카드 빌더·로그인 화면 완료 — **2순위 전부 완료.** 스프린트 문서 소진, 다음은 `EUM_INTEGRATION.md`.
> 진행(9/14, 2회차): 백로그 문서 3종이 모두 소진돼 **디자인 스프린트 이전에 만들어진 콘솔 탭 3개**(상담원 요청·감사 로그/저장소·테넌트 지식)를 같은 규격으로 끌어올렸다 — 아래 「2순위 후속」.
> 진행(9/14, 3회차): 「사업」 그룹 2개 탭(파트너·귀속, 정산 리포트)까지 같은 규격으로 — **콘솔 10개 탭 전부 스프린트 규격.** 남은 것은 [승인 필요] 항목뿐(약관 확정본).
> 진행(9/16): 보류했던 2건을 닫았다 — 평균 응답 시간 실측(DS 2-14)·헤더 전역 검색(DS 2-15). 「2순위 후속」에 기록.
> 진행(9/16, 2회차): 완료 기준으로 다시 훑어 남은 "개발자 초안" 흔적 3곳을 고쳤다 — 기본 Next 404/오류 화면, 불러오는 동안 먼저 뜨던 「자료가 없습니다」 빈 상태, 오프라인 무반응. 아래 「4순위 — 마감 점검」.
> 진행(9/16, 3회차): 랜딩·콘솔 밖에 남아 있던 두 곳 — 약관·방침 페이지(상단바 없이 본문만, 이모지 경고)와 링크 미리보기(OG 이미지·테마색 없음, 콘솔이 색인 가능) — 를 같은 규격으로. 아래 DS 4-4·4-5.

## 디자인 시스템 (AICC Portal 에서 이식)
- 색: primary #2563EB, ink #0F172A, sub #475569, line #E2E8F0, bg #F8FAFC, success #16A34A, warn #D97706, danger #DC2626
- 폰트 Pretendard, 라운드 14~18px 카드, 그림자 `0 1px 2px rgba(15,23,42,.04), 0 12px 30px -18px rgba(15,23,42,.18)`
- KPI 카드: 라벨 12.5px/값 30px 800/변화량 pill. 표: 헤더 11.5px 대문자 느낌, 행 hover
- 참고 파일: `Dev/2. Callbot/v2/callbot-portal/public/index.html`(랜딩·KPI·차트), `public/admin.html`(콘솔 셸·사이드바·표)

## 1순위 — 채팅 위젯 (이음 임베드 → 2R 평가자가 봄)
- [x] 헤더: 아바타(원형·브랜드색)·이름·「AI가 응대합니다」 배지·최소화/닫기. 테넌트 색상(`eum`=#BE5535) 적용
  - 근거(9/10): 36px 원형 아바타+테넌트 badge, AI 배지(점+라벨), 최소화(대화 유지)/닫기(초기화) 분리. `brandVars()`가 --brand 계열만 덮어써 렌더 HTML에 `--brand:#BE5535` 확인(tests/widget.test.mjs)
- [x] 말풍선: 봇/사용자 구분 색·꼬리·시각, **타이핑 인디케이터(점 3개)**, 도착 애니메이션
  - 근거(9/10): 봇=흰 카드+좌하 꼬리(radius 4)·사용자=브랜드+우하 꼬리, 말풍선 하단 시각(마운트 후 렌더 — hydration 불일치 방지), `.gw-dot` 점 3개(aria-label "답변을 작성하고 있습니다"), `.gw-rise` 도착 애니메이션 + prefers-reduced-motion 존중
- [x] 빠른 답장 칩(FAQ 상위 4개), 답변 하단 **근거 표시**(FAQ 번호·출처)와 「도움이 됐나요 👍👎」
  - 근거(9/10): `tenantConfig()`가 실제 적재 KB에서 상위 4건 질문을 그대로 내려보냄(문구 하드코딩 금지 테스트 동반). 근거 카드는 브랜드 좌측선+FAQ 라벨. 평가는 `/api/feedback`에 기록(근거 라벨·세션 해시만 저장, 대화 본문 미전송), 실패 시 재시도 안내
- [x] 상담원 전환 UI: 버튼 → 연락처 입력 카드 → 접수 완료 상태. 오류 시 재시도
  - 근거(9/10): 「상담원 연결하기」가 곧바로 접수하지 않고 카드를 연다 — 연락처 입력(전화/이메일 형식 검사)·이용 목적/파기 안내(`aria-describedby`)·「연락처 없이 접수」·취소. 접수 중에는 버튼 잠금+`aria-busy`(중복 접수 차단), 실패는 `role="alert"`+「다시 시도」, 완료 카드에 접수번호·상태·접수 순번. `/api/escalation` 의 `contact` 계약 그대로 사용(서버 무변경)
- [x] 모바일 375px 전체화면 모드, 키보드 포커스 트랩·ESC 닫기·aria-live
  - 근거(9/10): 480px 이하 전체화면 시트(임베드는 embed.js가 호스트 폭을 알려줌 — iframe 폭은 뷰포트가 아니므로). role=dialog + Tab 순환 + ESC 최소화 + 대화 목록 role=log/aria-live=polite, 닫은 뒤 초점은 런처로 복귀
- [x] `embed.js`: 로드 시 깜빡임 없음, `data-tenant`·`data-position` 옵션 유지
  - 근거(9/10): v0.5 — iframe을 `opacity:0` 으로 붙였다가 위젯이 보내는 `ready` 신호에 맞춰 페이드인. 신호가 막혀도 load+400ms·하드 2500ms 폴백으로 반드시 보인다(위젯이 사라지는 실패 모드 없음). `prefers-reduced-motion` 이면 전환 없이 즉시 표시. `data-tenant`·`data-position`·`data-offset`·`data-z` 계약 유지

## 2순위 — 관리 콘솔 (/admin)
- [x] 콘솔 셸: 좌측 사이드바(아이콘+라벨, 현재 탭 강조)·상단 헤더(테넌트·검색·관리자)·본문 카드 그리드
  - 근거(9/10): 가로로 늘어선 버튼 10개 → 236px 사이드바(로고·4개 그룹「운영/콘텐츠/사업/설정」·선 아이콘 10종·`aria-current="page"` 강조). 상단 스티키 헤더에 현재 화면 제목+한 줄 설명(`TAB_DESC`)·인증 배지·관리 토큰(보이지 않는 라벨 연결). 저장 결과는 인라인 문구 대신 토스트(`role="status"`)
  - 375px: 사이드바가 상단 가로 스크롤 메뉴로 접힌다(`.ac-navgroup{display:contents}`·그룹 제목 숨김·버튼 min-height 38px). 렌더 테스트 `tests/console.test.mjs`
  - 검색은 별도 항목으로 분리 → DS 2-15 에서 완료(헤더 전역 검색)
- [x] 대시보드: KPI 4(오늘 대화·자동완결률·상담원 전환·평균 응답) + **스파크라인/막대 차트**(Chart.js CDN 허용). 값 없으면 「측정 중」— 가짜 수치 금지
  - 근거(9/10): `convStats()` 에 실제 로그 기반 `daily`(최근 7일, 한국 시간 기준)·`today` 집계를 추가하고 KPI 4장(라벨 12.5/값 30px 800)을 그렸다. 평균 응답 시간은 수집 경로가 없어 「측정 중」, 자동완결률도 대화 0건이면 「측정 중」(0% 로 단정하지 않음)
  - 차트는 CDN 없이 인라인 SVG 막대(대화/상담원 제안 2계열, `role="img"` + 전체 수치를 담은 aria-label). 7일 내 기록이 없으면 차트 대신 「측정 중」 안내
  - 응답 근거·채널·인텐트를 내부 코드(`kb`/`web`) 대신 사람 말(「등록 자료」·「홈페이지」)로 표시 — 렌더 테스트에서 `intent `·`source ` 노출 0건 고정
- [x] 최근 대화: 목록 → 클릭 시 우측 **상세 서랍**(전체 대화·의도·출처·전환 여부)
  - 근거(9/10): 대시보드 최근 대화가 한 줄 요약 행(고객 말·챗봇 답·시각·주제·근거·상담원 제안)으로 바뀌고, 행을 누르면 우측 520px 서랍이 같은 대화(세션)의 흐름 전체를 말풍선으로 보여준다. 상단 요약 pill(메시지 수·자동 완료/상담원 제안·접수 상태), 하단에 「상담원 요청 보기」(해당 접수가 있을 때만 → 상담원 요청 탭). 대화 식별자는 앞 6자만 노출. 빈 상태는 일러스트+「응답 테스트 열기」
  - 접근성: `role=dialog`+`aria-modal`, 열리면 닫기 버튼으로 초점 이동, Tab 순환, ESC/배경 클릭으로 닫힘, 닫으면 연 행으로 초점 복귀. 375px 는 전체폭 시트·행은 세로 배치. `prefers-reduced-motion` 에서 슬라이드 제거. 서버 무변경(기존 `recentTurns` 30건 안에서 세션별로 묶음)
- [x] 지식베이스: 표(검색·태그 필터) + 우측 편집 폼, 저장 토스트, 빈 상태 일러스트
  - 근거(9/10): 카드 나열 → 좌 표(카테고리 pill·질문/답변 2줄 요약·출처·키워드·수정/삭제) + 우 스티키 편집 폼. 툴바에 검색(질문·답변·키워드·출처)과 카테고리 셀렉트(실제 등록값에서 생성), 건수 「n/전체」. 편집 중인 행 강조. 빈 상태 2종(자료 0건 → 일러스트+「기본 자료 불러오기」, 검색 0건 → 「필터 지우기」)
  - 폼: 모든 입력에 `<label>`, 필수(질문·답변) 인라인 오류+`aria-invalid`/`aria-describedby`, 저장 중 버튼 잠금(`aria-busy`), 네트워크 실패 안내, 식별자는 「고급」 접힘. 삭제·초기화는 `confirm` 을 거친다(QUALITY_BAR §3). 문서 붙여넣기 섹션은 표 아래로 이동(문구 사용자 언어로). 375px: 한 단으로 접히고 키워드 열 숨김, 수정 시 폼으로 스크롤
- [x] 시나리오 룰: 조건→응답 카드 빌더, 미리보기
  - 근거(9/14): 카드 나열 → 좌 「내가 만든 규칙」·「기본 규칙」 카드 목록(검색 툴바·건수). 카드마다 「고객이 이렇게 말하면」 표현 칩 → 화살표 → 「이렇게 답합니다」 흐름, 켜기/끄기는 `role="switch"`+`aria-checked`+이름 있는 스위치, 상담원 연결·답변 수정됨 pill. 기본 규칙은 정규식 대신 사람이 읽을 예시 표현 칩으로 풀고(`patternExamples`) 답변만 인라인 편집(벗어나면 저장 안내)
  - 우 스티키 빌더: 규칙 이름 → ① 표현(쉼표 구분, 입력 즉시 칩 미리보기) → ② 답변·상담원 접수 체크. 필수 3항목 인라인 오류(`aria-invalid`/`aria-describedby`), 저장 중 `aria-busy` 잠금, 네트워크 실패 안내, 삭제는 `confirm`. **미리보기**: 시험 문장을 적으면 어떤 표현으로 적용되는지(적중 칩 강조) + 고객에게 보일 말풍선(`role=log aria-live`)을 그리고, 근사치임을 밝히며 최종 판정은 「응답 테스트」로 안내. 서버 무변경
  - 내부 용어 제거: 「커스텀 룰」「정규식」「패턴: /…/」「(intent)」 화면 문자열 0건(감사 로그 라벨·복원 토스트 포함) — 테스트 고정. 375px: 조건/응답 세로 쌓임·화살표 회전, reduced-motion 시 스위치 전환 제거
- [x] 응답 테스트: 좌 입력/우 위젯 미리보기 분할
  - 근거(9/10): 좌: 「고객이 보낼 말」 textarea(Enter 전송·Shift+Enter 줄바꿈·IME 조합 중 전송 방지) + 「왜 이렇게 답했나」(주제·근거·인용·확신도 — `intent:`/`source:` 내부 코드 표기 제거, 라벨 사전 재사용). 우: 위젯과 같은 규격의 상담창 미리보기(브랜드 헤더·아바타·AI 배지·말풍선·근거 인용·`gw-dot` 타이핑 인디케이터, `role=log aria-live`). 대기 중 버튼 잠금, 네트워크 실패 시 말풍선으로 안내. 개인정보 주의 문구. 375px 한 단·미리보기 높이 300px
  - 실제 iframe 대신 같은 `/api/chat` 응답을 위젯 규격으로 그린다 — 테스트 세션이 위젯 세션과 섞이지 않고, 좌측 입력이 우측 화면을 직접 구동한다
- [x] **내부 문구 전부 제거**("data/admin-store.json", "401이면", "서버 메모리 기준"). 필요한 안내는 사용자 언어로 툴팁/설정 페이지에
  - 근거(9/10): 콘솔 부제·통계 로딩·통계 각주 3곳을 사용자 언어로 교체. 주석은 허용하되 **화면 문자열**에 내부 문구가 없음을 테스트로 고정
  - 덤: 「설치」 탭 신설 — 랜딩에 있던 임베드 스니펫을 옮기고 배포 주소는 `window.location.origin` 으로 생성(하드코딩 금지)·복사 버튼·옵션 표
- [x] 관리 토큰 입력은 「로그인」 화면으로 (ADMIN_AUTH_REQUIRED 는 여전히 [승인 필요])
  - 근거(9/14): 헤더의 토큰 입력칸 제거 → 상태 배지(로그인됨/로그인 전/인증 미설정) + 「로그인」/「로그아웃」(토큰 삭제 후 로그인 화면). 잠금 화면(🔒 텍스트)을 브랜드 마크·제목·라벨 있는 토큰 입력(보기/숨기기 `aria-pressed`)·`<form onSubmit>`(Enter)·오류 `role=alert`+`aria-invalid`·확인 중 `aria-busy` 인 로그인 카드로 교체. 인증이 필수가 아닌 환경에서는 「로그인하지 않고 돌아가기」. 내부 문구(localStorage·401) 0건. 기존 `/api/admin/auth` 계약·잠금(lockout) 그대로

## 2순위 후속 — 스프린트 이전에 만든 탭 (9/14 진단: 카드 나열·내부 문구·세션 원문 노출)
- [x] 상담원 요청: 요약 KPI 4 → 상태 필터 칩 + 검색 → 표 → 접수번호 클릭 시 상세 서랍
  - 근거(9/14): 티켓마다 카드 → 요약(대기/상담 중/완료/자동 응대 완료율 — 대화 0건이면 「측정 중」) + 표(접수 앞 8자·상태 pill·고객 마지막 말 2줄·사유·시각·다음 처리 버튼 1개). 상태 필터는 `aria-pressed` 칩, 검색은 라벨 있는 `type=search`, 건수 `aria-live`. 서랍(`role=dialog`·ESC·Tab 순환·초점 복귀)에 고객 말풍선·사유·이관 요약·**연락처 마스킹(「보기」를 눌러야 원문)**·상태 변경(`aria-busy`)·「대화 전체 보기」(대화 서랍으로 연결). 내부 문구 「서버 메모리에만 저장됩니다」 제거 → 저장 미승인일 때만 사용자 언어 안내. 세션 원문·티켓 전체 id 노출 0건. 375px: 사유·시각 열 접힘·서랍 전체폭
- [x] 감사 로그·저장소 상태: 표(시각·작업 pill·대상/내용·인증 pill) + 작업 종류 필터 + 저장소 네임스페이스 카드 그리드(상태별 색)
  - 근거(9/14): 이벤트마다 카드 → 표. 빈 상태 일러스트+「지식베이스 열기」, 필터 0건 「필터 지우기」. 저장소 힌트의 환경변수명(`ADMIN_PERSIST`·`PERSIST_PII`) 제거 → 사람 말 + [승인 필요] 표기. 테스트가 화면 문자열의 환경변수명·내부 경로 0건 고정
- [x] 테넌트 지식: 요약 카드 4(상담창 이름+브랜드 색 견본·적재 FAQ·신청 버튼 주소와 출처 pill·AI 고지) + FAQ 표(근거 라벨·질문/카테고리·답변 2줄·표현 수)
  - 근거(9/14): `data/<테넌트>-faq.json` 경로 문구 제거. 0건은 경고 빈 상태. 편집 입력 없음(읽기 전용 유지 — 테스트 고정). 「코드 기본값 — 배포 환경변수 미설정」 → 「기본값 — 배포 설정 미등록」
- [x] 파트너·귀속: 요약 KPI 4 → 좌 고객사 표(검색·귀속 필터)·파트너 표 / 우 스티키 폼(고객사↔파트너 전환) → 고객사명 클릭 시 상세 서랍(계약 정보·귀속 이력 타임라인)
  - 근거(9/14): 세로로 쌓인 카드 6장(집계·폼 2개·목록 2개) → KPI(운영 중 파트너·고객사·계약 중·검토 중, 불러오기 전 「측정 중」) + `ac-split`. 고객사 표는 상태 pill·귀속·유입 경로·계약일·월 이용료(미입력은 흐리게), 파트너 표는 상태 pill·수수료율·담당·귀속 고객사 수(집계 표를 파트너 표에 합침, 직접 계약은 마지막 행). 식별자 원문(`p.id`·`a.id`·이력의 partnerId) 화면 노출 0건 — 전부 이름으로. 「bp」 단위 제거 → 수수료율은 %로 입력하고 저장 시 bp 변환(API 계약 무변경)
  - 폼: 고객사/파트너 세그먼트(`aria-pressed`), 모든 입력에 `<label>`, 인라인 오류 6종(`aria-invalid`/`aria-describedby` — 고객사명·파트너 유치면 파트너 필수·계약일 형식·계약 상태면 계약일 필수·월 이용료 숫자·수수료율 0~100), 계약일은 `type=date`, `<form onSubmit>`(Enter), 저장 중 `aria-busy` 잠금, 네트워크 실패 안내. 수정 시 해당 행 강조(`data-editing`)+폼으로 스크롤. 서랍은 `role=dialog`·ESC·Tab 순환·초점 복귀·「수정」으로 폼 연결. 빈 상태 3종(고객사 0·파트너 0 → 「첫 … 등록」, 필터 0건 → 「필터 지우기」). 조회 전용 계정은 폼·수정 버튼 숨김. 375px: 유입 경로·계약일·월 이용료·담당 열 접힘, 폼은 표 아래 한 단. 테스트 `tests/console.test.mjs` DS 2-12
- [x] 정산 리포트: 조건 툴바(기준월·파트너·다시 계산·CSV) → 요약 KPI 4 → 파트너별 합계 표 → 고객사별 산출 근거 표(미산출 사유 pill) → 빈 상태
  - 근거(9/14): KPI(대상 고객사·산출 완료·미산출·수수료 합계) — 리포트 전 「측정 중」, 산출 건이 없으면 합계를 0원으로 보이지 않고 「대상 없음/산출 불가」, 부분 합계는 카드 각주와 경고 카드로 「확정 금액 아님」 명시. 금액은 tabular-nums·우측 정렬, 미산출 건은 경고 pill. 빈 상태는 「파트너·귀속 열기」로 연결, 오류는 `role=alert`+「다시 시도」, 계산 중 `role=status`+버튼 `aria-busy`, 내려받을 것이 없으면 CSV 잠금. 하드코딩 색(#c0392b·#b26a00) 제거. 375px: 계약일·월 이용료·수수료율·기준금액 열 접힘. 테스트 DS 2-13

- [x] 평균 응답 시간 KPI 실측 연결 (DS 2-14)
  - 근거(9/16): `/api/chat`·카카오 웹훅이 서버 처리 시간(`latencyMs`, 네트워크 왕복 제외)을 대화 로그에 기록하고 `convStats()`가 `avgLatencyMs`(표본 0이면 null — 0ms 로 단정하지 않음)·`latencySamples`를 집계. 대시보드 KPI 는 값이 있을 때만 `latencyLabel` 로 표시하고 각주에 「서버가 답을 만드는 데 걸린 시간 · 최근 n건 평균」을 밝힌다. 음수·NaN 지연은 기록 단계에서 버린다. 구버전 로그(지연 없음)는 평균에서 제외. 테스트 `tests/console.test.mjs` DS 2-14 + 런타임 집계 테스트
- [x] 헤더 전역 검색 — 대화·안내 자료·규칙·상담원 요청·고객사·파트너 (DS 2-15)
  - 근거(9/16): `GlobalSearch` — WAI-ARIA combobox(`role=combobox`·`aria-expanded`·`aria-controls`·`aria-activedescendant`) + `role=listbox/option` 결과, 출처별 그룹, ↑↓/Enter/ESC, Ctrl+K 단축키(장식 힌트는 `aria-hidden`, 스크린리더에는 사용법 문장), 건수 `role=status`. 결과를 고르면 해당 탭으로 이동해 서랍/편집 폼을 연다. 고객사·파트너는 검색을 처음 열 때 한 번 불러온다
  - 개인정보: 연락처·세션 원문·접수번호 전체는 색인하지 않는다(요청 서랍에서 「보기」를 눌러야 원문) — 테스트가 색인 함수 본문을 검사해 고정. 375px: 검색칸이 헤더 아래 한 줄 전체폭, 키보드 힌트 숨김

## 3순위 — 랜딩 (/)
- [x] AICC Portal 랜딩 구조 이식: 히어로 → 동작 방식(신뢰 3원칙) → 기능 6 → 채널 3종 → 도입 3단계 → 운영 콘솔 미리보기 → FAQ → CTA
  - 근거(9/10): 상단바(스티키·앵커·CTA) 신설, 히어로에 대화 예시 카드(「화면 예시입니다」 명시) + 실제 위젯은 페이지에 상시 떠 있음. 콘솔 미리보기 KPI 4는 전부 「측정 중」
  - 벤치마킹: 채널톡 알프(자료 등록→근거 기반 응답·멀티채널·즉시 체험), Intercom Fin(답변 근거 인용·전환/해결 지표 중심 설득)
  - **운영자용 정보는 랜딩에서 제거**(사용자 요청 9/10): 임베드 스니펫·`/admin` 링크 → 관리 콘솔 「설치」 탭. 테스트로 재유입 차단
- [x] 브랜드: 제품명을 **GOWON Chat**(영문)으로 통일 + 말풍선 마크 신규 제작. 「AICC 제품군」 배지는 제거
  - 근거(9/10, 사용자 결정): 화면·타이틀·위젯 헤더·약관/방침·임베드 주석까지 「고원 챗봇」 표기 0건. 법인명 「주식회사 고원(GOWON)」은 약관·방침에 그대로 둔다
  - 아이콘: 말풍선 몸통 + 이니셜 G 를 `src/app/icon.svg`(파비콘)로 만들고, 랜딩 상단바·콘솔 사이드바가 **같은 경로 데이터**를 쓰도록 테스트로 고정(브랜드가 화면마다 달라지지 않게). 위젯 기본 아바타는 `G`(테넌트는 자기 배지 유지)
  - 이모지 아이콘(📎🤐🙋📚…) 전량 제거 → 콘솔 메뉴와 같은 굵기(1.4)의 선 아이콘 세트로 교체. 문구도 기계적인 표현을 걷어냈다(「상담 운영에서 실제로 쓰는 것만」·「오픈까지 세 단계」·「도입 전에 많이 받는 질문」 등)
- [x] 가짜 수치·타사 로고 금지(§13). 성능 수치는 기능 서술로
  - 근거(9/10): 「효과는 얼마나 되나요?」 FAQ에서 실측 전 수치 제시를 거절하고 파일럿 집계로 안내. 성과 수치 패턴·화면 내 타사명은 테스트로 금지

## 4순위 — 마감 점검 (9/16: 완료 기준 「같은 회사 제품으로 보이는가」로 다시 훑음)
- [x] 404·오류 화면 브랜드화 — 기본 Next 화면(흰 바탕 영문) 제거 (DS 4-1)
  - 근거(9/16): `src/components/SystemPage.tsx`(카드 규격·브랜드 마크·`h1`+`aria-labelledby`·버튼 42px·줄바꿈) 를 `app/not-found.tsx`(404, 「홈으로」)·`app/error.tsx`(「다시 시도」=reset·「홈으로」)·`app/global-error.tsx`(루트 레이아웃 실패 시 — globals.css 가 없어 토큰 인라인·`lang=ko`) 가 공유. 마크는 `src/components/BrandMark.tsx` 로 파비콘과 같은 경로 데이터(브랜드 테스트에 포함)
  - 오류 내용(메시지·스택·경로)은 화면에 싣지 않고 참조 번호(`digest`)만 — 테스트가 `error.message`/`error.stack` 사용 0건 고정. 404 는 운영자 콘솔 경로를 안내하지 않는다(랜딩과 같은 원칙). 브라우저 오류 보고 훅 연결은 모니터링 DSN 승인 뒤 — [승인 필요]
- [x] 로딩 스켈레톤 — 불러오는 동안 「자료가 없습니다」 빈 상태를 먼저 보이지 않는다 (DS 4-2)
  - 근거(9/16): 데이터 4종(안내 자료·규칙·상담원 요청/현황·변경 이력)에 `phase: loading|done|error` 를 두고, 빈 상태는 `done` 일 때만 그린다. 그 전엔 `SkeletonRows`(막대는 `aria-hidden`, 문장 하나만 `role=status aria-live=polite aria-busy`), 대시보드는 KPI 값 자리·7일 차트 막대까지 스켈레톤. 「측정 중」은 데이터가 *없을 때* 로 의미를 좁혔다(불러오는 중 ≠ 측정 중)
  - 실패 경로: 로더 4개에 try/catch 를 붙여 네트워크 예외가 조용히 사라지지 않게 — `phase=error` 면 `role=alert` + 「다시 시도」. 헤더 토큰 입력이 사라진 뒤에도 남아 있던 「위쪽 관리 토큰을 확인해 주세요」 안내 제거. 파트너·테넌트 탭의 텍스트 로딩도 스켈레톤으로. `prefers-reduced-motion` 에서 반짝임 제거, 375px 차트 스켈레톤 높이 축소. 테스트 DS 4-2(첫 렌더에 빈 상태 0건·상태 기록·게이트 4곳)
- [x] 오프라인 안내 배너 (DS 4-3)
  - 근거(9/16): `online/offline` 이벤트로 헤더 아래 경고 톤 배너(`role=status aria-live=assertive`) — 「인터넷 연결이 끊겼습니다. 화면은 볼 수 있지만 저장·새로고침은 연결이 돌아온 뒤에 됩니다」. 복구되면 사라진다. 서버 렌더에서는 그리지 않는다(연결 상태 미확인). 테스트 DS 4-3
- [x] 약관·개인정보처리방침 페이지 셸 통일 (DS 4-4)
  - 근거(9/16): `LegalLayout` 이 「← 홈」 링크 하나짜리 본문에서 랜딩과 같은 상단바(브랜드 마크 공용 컴포넌트·「홈으로」)·카드(눈썹 라벨·제목·시행일/운영주체/초안 pill)·푸터(약관↔방침 교차 링크)로. 본문 `<h2>` 를 자동으로 색인해 좌측 스티키 **목차**(`nav aria-label="목차"`, `scroll-margin-top`)를 만든다 — 페이지 파일은 제목만 쓰면 된다. ⚠️ 이모지 → 선 아이콘, 하드코딩 색(#9a3412·#ffedd5…) → 토큰(`--warn`) — 테스트가 TSX 의 hex·이모지 0건 고정. 문안(초안·[미확정] 표기)은 그대로 — 확정본 반영은 여전히 [승인 필요]
  - 375px: 목차가 카드 위 가로 스크롤 칩(최소 높이 34px)으로, 카드 여백 18px. 운영자 화면(/admin) 링크 없음
- [x] 링크 미리보기·색인 메타데이터 (DS 4-5)
  - 근거(9/16): 루트 레이아웃에 `metadataBase`(`NEXT_PUBLIC_SITE_URL`, 미설정 시 배포 주소)·Open Graph(`ko_KR`·siteName)·트위터 카드·`viewport.themeColor`(=`--brand`). 미리보기 이미지는 `app/opengraph-image.png`·`twitter-image.png`(1200×630, 파일 규약·대체 텍스트 동반) — `scripts/og-image.py` 가 브랜드 마크 도형·랜딩 히어로 문장·신뢰 3원칙으로 그린다(수치·타사명 없음, 테스트 고정). 관리 콘솔(`admin/layout.tsx`)·임베드 프레임(`/widget`)은 `robots noindex`, 랜딩·약관·방침은 색인 허용

## 완료 기준
QUALITY_BAR 6조건 + **"AICC Portal 옆에 두었을 때 같은 회사 제품으로 보이는가"**. 각 항목은 375px·키보드·스크린리더 확인 후 `[x]`.
